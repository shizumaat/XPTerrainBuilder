# Object-derived terrain features — tunnels, below-grade structures, and taxiway bridges

**Reads with:** [`docs/dsf_object_integration_spec.md`](dsf_object_integration_spec.md) (the OBJ8
reader/footprint/y-bake machinery this feature extends) and
[`docs/dsf_object_anchor_plan.md`](dsf_object_anchor_plan.md) (the anchor physics — §4 and §7.3 are
prerequisites for every agent touching geometry here). This document is the *design and build order*
for two new object classes: **tunnels / below-grade structures** (feature A) and **taxiway bridges**
(feature B).

**Evidence base:** the 2026-07-09 three-pack investigation (EGLL TaiModels, Nimbus KBNA + Nashville
Roads, Aviotek EDDF) and the dissection of the author-supplied EGLL base mesh (§2.4). Probe scripts
from that session: `tunnel_probe.py`, `rim_probe.py`, `mouth_probe.py`, `bridge_probe.py`,
`aptdat_bridge_scan.py`, `pavement_coverage.py` (session scratchpad; promoted probes land in
`tools/` per W-V).

**Baseline:** `dev` (post 9345739, runway-end skirt anchored at the runway end). Working-tree state
of `adjacent_ground.py` (node-split welded walls) is a dependency of feature A emission.

**Shape of the work:** one shared classifier library feeding two independently gated terrain
features.

| | Feature A — tunnel cutouts | Feature B — bridge adaptation |
|---|---|---|
| what | flatten band over roofed body + open trench at the mouths | terrain meets the deck ends; depressed road corridor under the span |
| exemplar | EGLL (20 tunnel objects) | KBNA taxiway L (deck-carried), EDDF taxiway P (terrain-carried) |
| solver involvement | flat anchor seat at the datum; pins/ceilings where a rim abuts pavement | deck-end hard pins (seam-pin idiom); everything else lawful grading |
| emission | late block, skirt-style, welded node-split walls | existing underpass-corridor emitters, re-sourced from object geometry |
| flag | `O4_OBJECT_TUNNEL_TERRAIN` (default off) | `O4_OBJECT_BRIDGE_TERRAIN` (default off) |

---

## 0. Rulings in force

Decided; do not relitigate. If implementation proves one wrong, stop and escalate.

**R1 — One-solve doctrine (user, 2026-07-09, chain-identity session).** All laws live in
`grade_law.py`; minimize post-solve mutation; solver performance is first-class. Feature B's
deck-end coupling therefore lives *inside* the solve as pins, not as a post-solve rewrite. Feature
A's trench is off-pavement terrain (outside the solver's domain) and is emitted, but its *law* — rim
and floor elevations as a function of datum and deck profile — is a `grade_law` function imported by
both the emitter and the validator (lockstep, same as the runway-end skirt).

**R2 — Weld ruling (user, 2026-07-09).** Emitted strips weld fully to pavement; deliberate
divergence only via node-split walls (two coincident node rows carrying different values). Trench
rims weld to surrounding terrain values; the vertical drop is a node-split wall. Pavement value
always wins at any contact.

**R3 — No true mesh holes.** The mesh is a 2.5-dimensional heightfield; Triangle's hole plumbing is
unused and untested end-to-end. Feature A models *open-cut trenches*, never voids. §2.4 records what
the author's own mesh does; even if X-Plane tolerates base-mesh holes, we do not emit them in this
iteration (revisit only with a user ruling).

**R4 — Phase-2 interlock (designer, this spec; consequence of R1/R2).** An object whose terrain is
carved or seated to match it (any structure consumed by feature A or B) is **excluded from the
Phase 2 y-bake**. Terrain-to-object and object-to-terrain corrections must never stack. The
classifier emits the exclusion list; `post_mesh.rebake_dsf_objects` consumes it.

**R5 — Contract before shape (designer, this spec).** No bridge terrain is emitted until the
structure's contract is classified (deck-carried versus terrain-carried, §3.2). Ambiguous
classifications (pavement coverage between the thresholds, or no `ATTR_hard_deck` found) are
refused and reported, never guessed.

**R6 — Pack fixtures are synthesized, never copied.** Third-party pack content (OBJ8, DSF, apt.dat)
must not enter the repository. Test fixtures are minimal synthetic files generated by the tests
themselves, shaped like the real signatures (roof-at-zero shell + sunken deck; hard-deck plane +
abutments).

**R7 — No abbreviations in identifiers** (user, standing, all projects). `heading_degrees`, not
`hdg`; config-constant unit suffixes `_M` / `_M2` remain the established exception.

**R8 — Hard-deck flush seating (user, 2026-07-09).** When the pack provides a hard deck object
(`ATTR_hard_deck`), **our pavement is cut over that object's footprint** so the object sits flush
and carries the drivable surface: tunnel roofs (pavement cut over the trench body — the roof is
the ground there) and deck-carried bridge spans (pavement cut between the abutment lines, the
pattern the KBNA author already uses). This resolves open question 5: no pavement is left
coplanar over a hard roof slab. A **non-hard** roof or deck gets no cut — pavement wins (R2) and
the object stays cosmetic beneath it. **Amended by R13** (owner, 2026-07-30): a modelled OPEN PIT
is also cut, hard deck or not — it has no deck at all, and pavement drawn across a hole is not
cosmetic.

**R9 — The vertical split is derived, never assumed (user requirement, 2026-07-09).** Bridges
divide the taxiway-to-road separation between road depression and taxiway rise differently per
airport (KBNA: road fully depressed, deck level with the taxi network; EDDF: terrain ramps up to
an elevated span over a partially lowered highway; KMCO/KDFW: flat surrounding base, the bridge
itself rises over a road lowered little or not at all). The model must express the whole family:
the split is read from the object's deck profile and the road's actual elevation state (§3.2),
never hard-coded per style.

**R10 — Interior cutouts under split-depth buildings (user, 2026-07-09).** A building that is
mostly at grade but has portions extending below gets an **interior mesh cutout**: the mesh is
carved inside the structure's at-grade footprint (perimeter stays at grade where facade bases
are at grade) down below the below-grade content, so modeled underground spaces are visible
from INSIDE the building. Exemplar: KDEN's underground train stations beneath each terminal.
This refines A6's buried-by-default: burying remains correct for un-enclosed below-grade slack
(the EGLL jetway class) and where pavement covers the piece, but enclosed below-grade interiors
are cut by default — harmless from outside (hidden by the building), necessary at KDEN. Guards
(calibrated by the KDEN measurement pass, amendment pending): cut strictly inside the at-grade
footprint; never carve pavement; enclosure of the below-grade piece within the parent
structure's at-grade footprint is the trigger. Corrected exemplar note (user): **LFPG Terminal 1
is genuinely set below apron grade** — a sub-grade level with the road deck nearer apron level
above it, so the whole terminal area is cut below apron grade (A5's "foundation moat parasite"
reading was wrong); the multi-level-central-spine pattern is **Terminal 2**. The user's
hand-built patch (`Ortho4XP-1.40.12/Patches/+40+000/+49+002/LFPG.patch.osm`) is the reference
treatment; a measurement pass against it is in flight.

**R11 — Correlating custom mesh as cutout oracle (user, 2026-07-09).** When a custom base mesh
exists and correlates with the custom airport pack (the EGLL_MESH case), consulting it to find
and confirm cutouts is useful and **allowed**: buried-versus-exposed disambiguation, cutout
footprints and depths, and datum reference. This promotes the A6 correlation methodology from a
one-off audit to a sanctioned input — an oracle module samples the author mesh where present
and (a) confirms/overrides classifier calls (the EGLL `4.obj` bridge-versus-buried case, the
KDEN Jeppesen escalator case), (b) supplies cutout footprints the objects under-specify. Object
evidence remains the primary path; the oracle refines where it exists. Open question 1's datum
half (copy the author elevation versus solve our own) remains open — the oracle *reports*
datum deltas, it does not yet impose them.

**Agents additionally must not:** run `git add`/`git commit` (the supervisor reviews and commits);
edit files owned by another workstream (§5 ownership map); write outside the repository and the
session scratchpad.

---

## 1. Scope and non-goals

**In scope**

* A structure classifier (`src/auto_patch/object_terrain_features.py`) recognizing tunnel and
  bridge structures from OBJ8 geometry plus placements, and classifying each bridge's terrain
  contract.
* Placement-reader extensions: `OBJECT_MSL` rows (currently skipped — at KBNA they are the only
  absolute elevation source) and negative `OBJECT_AGL` offsets as a below-grade signal.
* A DSF road-network reader (`BEGIN_SEGMENT` / `SHAPE_POINT` with the draped-versus-elevated level
  flag) as a road source alongside OpenStreetMap — the KBNA bridged roads exist only there.
* Feature A: flatten seat + mouth trenches for below-grade structures (EGLL class).
* Feature B: deck-end pins + object-sourced underpass corridors (KBNA class); at-grade continuity
  for terrain-carried decks (EDDF class).
* Replacement of the boolean `_scenery_has_bridge_objects` name-grep (misses every EDDF crossing)
  with classifier output.
* Feature C (measurement-gated, A3): split-level structure interfaces — per-facade ground-contact
  levels extracted from terminal/station objects as elevation evidence for
  airside/groundside/roadway/rail grade separation (ELLX / LFPG / LFLL class).
* Verification audits promoted into `tools/`.

**Explicitly out of scope**

* True mesh holes (R3).
* The EDDF cosmetic swale (depressing terrain about 1 m inside trench-cladding interiors). The
  terrain-carried contract needs *flat grade across the deck*, which the solver already produces;
  the swale is cosmetic-only and lands later behind its own flag if the user wants it.
* Roofed-bore rendering, road traffic, or any DSF rewriting.
* Draped-cosmetics rescue (facade guardrails, painted lines, forests sinking into depressed
  corridors). Known, logged by the emitters, fixed in a follow-up.
* Re-anchoring the deep below-grade *building* geometry found at EGLL (`T2_T3_3.obj` at −9.2 m and
  kin) — that is Phase 2 worklist material, not a terrain feature.

---

## 2. Evidence and the physical model

### 2.1 EGLL tunnels (feature A exemplar)

Twenty `Airport/Tunnel/*.obj`, one placement each, **all heading 0**. Pairing convention: `N.obj` =
shell (roof top flush at local y = 0, walls to −4…−7 m); `Na.obj` = road deck (flat at −4…−5 m,
ramps rising to 0 at the mouths). Cut-and-cover: only the mouths are open to the sky. Seventeen
plain `OBJECT`; three `OBJECT_AGL` with negative offsets (−1.0, −7.0, −7.5 m) — above-grade
geometry placed below grade, invisible to a vertex-depth filter alone.

**Mouth derivation (validated):** plane-sectioning the shells fails (open shells, duplicated seams
— curve soup, 0–2 closed of 34–54). **Union of projected up-facing triangle footprints** succeeds
by construction: mouths = deck footprint − roof footprint, yielding clean 4–21-vertex polygons
(tunnel 2: four mouths, 890–5,052 m²). Depth inside each mouth comes from the deck triangles it
contains.

**Signatures (A6 — mesh-validated, replacing the depth heuristic):** a structure is a tunnel
(cut a trench) **iff** it has a *drivable* surface (`ATTR_hard` / `ATTR_hard_deck`) whose
triangles reach below effective grade, OR a negative `OBJECT_AGL` offset ≤ −1 m. Depth alone is
**exactly inverted** as a signal: the deepest below-grade geometry in the pack is buried
building basements (`T2_T3_3` at −9.22 m), while exposed tunnels run only −4 to −5 m. Validated
against the author mesh: zero wrong on all 30 below-grade building objects and all 20 tunnels.
`Tunnel/` naming remains a secondary hint only.

### 2.2 KBNA bridges (feature B, deck-carried contract)

Pavement (all DSF draped polygons; the apt.dat has zero code-110 rows) is **cut at the abutments**
— mid-deck pavement coverage 0%. The `ATTR_hard_deck` deck at +5.99 m above anchor terrain carries
the taxiway; the road drapes level-0 beneath (polylines in the sibling Nashville Roads pack's DSF
road network). Taxiway L: 131 × 55 m deck, heading 108°, girder underside +4.2 m. Twelve
`OBJECT_MSL` centerline fixtures at 166.9994 m pin the deck top absolutely → deck 167.0 m, corridor
161.0 m above sea level. The Murfreesboro service bridges have **no** hard deck (cosmetic): trucks
ride the terrain, so the causeway-plus-corridor shape is mandatory there.

### 2.3 EDDF bridges (feature B, terrain-carried contract)

Hard decks flush at y = −0.02; apt.dat pavement drapes **continuously across the spans** (30–78%
of the deck box); paired `Tunnel_N` cladding has its visible floor at only −1.0 m (headroom is
texture) with wall skirts to −6.02 m. Terrain must stay at taxiway grade across the deck. The
`sim/exclude_net` zones sit exactly under two of the crossings; under the others the live road is
in the default base mesh, outside the pack.

**Contract disambiguation (validated on both packs, refined by A4):** pavement-coverage fraction
of the mid-deck box — ~0% ⇒ deck-carried; above ~30% ⇒ pavement-continuous — cross-checked by
road-network level flags beneath. Pavement-continuous then splits on the measured deck profile:
flat ⇒ **terrain-carried** (EDDF flush decks); non-flat (sloped/crowned) ⇒ **profile-carried**
(KMCO humps, the EDDF A3 ramp) — the deck profile is the pavement's elevation target, so "flat
across" handling would erase the hump. Thresholds are config constants; the band between the
coverage thresholds is refused per R5. Two more mechanical guards from the A4 measurement pass:
drivable surfaces may be `ATTR_hard` rather than `ATTR_hard_deck` (KMCO and the EDDF ramp carry
zero `hard_deck` triangles — keying on `hard_deck` alone misses every humped bridge measured),
and **piered viaducts are excluded by the abutment-reaches-grade test** (solid geometry must
reach local y ≈ 0 at each profile end; KMCO's rail viaduct never comes below +3.45 and would
otherwise induce a false causeway).

### 2.4 The author's own EGLL mesh — datum and cutout ground truth (amendment A1)

Dissection of `c_GBR - 100_airport - EGLL_MESH/Earth nav data/+50-010/+51-001.dsf` (MeshTool-made
per `sim/creation_agent`, per-vertex integer-metre elevations, triangle strips + lists, stock
terrain definitions). Four findings bind this design:

1. **No holes exist.** Every tunnel footprint has 100% triangle coverage (grid-sampled,
   thousands of points per tunnel). The cutouts are **depressed open trenches with near-vertical
   walls** — a grade vertex and a deck vertex at nearly the same longitude/latitude (measured
   drops of 21 m → 15 m within ≤ 1 m horizontal). This is exactly our node-split wall mechanism
   (R2); the file does **not** demonstrate sim tolerance of base-mesh holes, so R3 stands on both
   grounds.
2. **The author cuts the ENTIRE tunnel body, not the mouths.** Tunnel 2's trench floor runs
   continuously at 15 m for the full ~1,030 m body (bbox longitude −0.487057..−0.471960); our
   four derived mouth polygons are subsets of that one cut. The at-grade surface over the body is
   supplied by the overlay pack's roof *object* (top at local y = 0), not by terrain. Rationale
   this design adopts: terrain left at grade under a roof slab whose top is also at grade would be
   coplanar with it and z-fight; cutting the body away makes the roof object the visible ground.
3. **The trench floor sits below the object's road deck, never at it.** Author floors: tunnel 2
   at 15 m where the object deck renders at 16 m (datum 21 − 5); tunnel 9 at 16 m versus deck
   16.5; tunnel 11 at 14 m versus deck 14–15. The mesh never carries the visible road — the
   OBJ8 deck does — the mesh only stays safely beneath it. Floors are flat pans (no longitudinal
   road grade; the object's ramps do the vertical work), quantised to whole metres.
4. **Datum.** A hand-flattened platform at exactly **21 m** covers 97.6% of the airport's
   non-water ground (about 2 m below the author's own source raster; Heathrow's published
   elevation is 25 m); both runways are dead flat at 21.0 m end to end. Tunnel anchors: tunnels
   2, 9, 10, 11 → **21 m**; tunnels 6/7 (northwest rising ground) → **28 m**. These are the
   reference values `tunnel_trench_audit.py` reports deltas against (open question 1: we do not
   copy the author's datum, we solve our own — the audit quantifies the disagreement).
5. **Below-grade building geometry is 100% buried (A6 correlation).** Every below-grade piece
   of every non-tunnel object — all 30 resources, including basements to −9.22 m and 194 jetway
   wheel-slack placements — sits under mesh that runs flat at the platform datum. The author
   exposes *only* drivable tunnel decks. The single non-tunnel interior depression is a 2 m
   T5 landside forecourt sub-platform that still buries everything beneath it by 1.5–3.2 m.
   Corollary confirmed: tunnels are **mesh-walled** (the heightfield makes the vertical drop;
   the object supplies the deck), buildings are the opposite (mesh runs flat underneath, the
   object carries its own invisible below-grade walls). The author's floor sits ~1.0 m below
   the OBJ8 road deck (integer-quantised mesh); our `TUNNEL_FLOOR_BELOW_OBJECT_DECK_M = 0.5`
   satisfies the same strictly-below contract at finer precision.

### 2.5 Why the solver cannot own the terrain shapes

The elevation solver's domain is pavement-network nodes only; off-pavement terrain is written by
post-solve emitters as per-vertex altitudes and baked by Triangle4XP into a continuous heightfield.
"Put the geometry in place and let the solver handle it" therefore covers exactly the parts of
these features that *are* pavement-coupled — deck-end pins, rim-adjacent pavement ceilings, the
flat anchor seat — and nothing else. This is the same split the underpass corridors, retaining
walls, and runway-end skirts already live with.

---

## 3. Design

### 3.1 Shared classifier — `object_terrain_features.py`

Consumes the placement list (extended per W-R1) and per-object OBJ8 geometry (existing
`obj8_reader` loaders). Produces:

```
TunnelStructure:
    object_resources          # shell + deck file(s), grouped by shared anchor/adjacency
    anchor_longitude_latitude, heading_degrees, placement_kind, above_ground_offset_m
    roof_footprint            # union of up-facing triangles with top near y = 0 (world frame)
    deck_footprint            # union of up-facing triangles below the roof
    mouth_polygons            # deck_footprint − roof_footprint, split to parts
    mouth_depth_samples       # per mouth: deck-triangle y statistics (ramp profile)
    body_depth_m              # deck level of the roofed body

BridgeStructure:
    object_resources, anchor_longitude_latitude, heading_degrees
    deck_polygon                         # plan footprint of the ATTR_hard_deck surface
    deck_top_profile                     # deck top y sampled along the bridge axis (~10 m bins):
                                         #   flat (KBNA), single value; sloped or crowned
                                         #   (KMCO/KDFW class), the full profile — R9
    deck_top_y_m                         # convenience: profile maximum (crest)
    deck_end_elevations_y_m              # profile value at each end — the solver pin values
    ceiling_y_m                          # largest down-facing plane below the deck (clearance)
    abutment_lines                       # deck-end segments where solid geometry reaches y ≈ 0
    abutment_reaches_grade               # per end: solid geometry reaches y ≈ 0 there (A4);
                                         #   an end that never reaches grade ⇒ piered viaduct,
                                         #   refused (no terrain feature)
    contract                             # DECK_CARRIED | TERRAIN_CARRIED | PROFILE_CARRIED
                                         #   | AMBIGUOUS (A4)
    absolute_deck_elevation_m            # from OBJECT_MSL fixtures on the deck, else None
                                         #   (KBNA is the only pack measured that has them)
    hard_deck                            # bool; False ⇒ cosmetic (Murfreesboro class).
                                         #   Detection accepts ATTR_hard_deck OR near-horizontal
                                         #   ATTR_hard planes (A4)

StructureGroundInterface (feature C, A3):
    structure reference (shared with the building-pad pipeline)
    perimeter_base_profile               # minimum solid y binned along the footprint boundary
    interface_levels                     # clustered: [(level_y_m, boundary_arcs, perimeter_share)]
    split_level                          # bool; False ⇒ the existing single flat pad (R5) stands

exclusions: [(pack_root, object_resource)]   # R4 feed for the Phase 2 y-bake
```

Detection thresholds (config constants, no magic numbers in code):
`TUNNEL_MIN_BODY_DEPTH_M = 2.0`, `TUNNEL_ROOF_TOP_TOLERANCE_M = 0.5`,
`BRIDGE_MIN_DECK_AREA_M2 = 200`, `BRIDGE_DECK_CARRIED_MIN_HEIGHT_M = 2.0`,
`BRIDGE_CONTRACT_PAVEMENT_COVERAGE_DECK_CARRIED_MAX = 0.05`,
`BRIDGE_CONTRACT_PAVEMENT_COVERAGE_TERRAIN_CARRIED_MIN = 0.30`.

The classifier is pure (geometry in, records out) and independently testable against synthetic
fixtures (R6). It runs at layout time — Phase 1 already proves OBJ8 data is available there.

### 3.2 Feature B — bridges

**Deck-carried (KBNA class).**
1. *Deck elevation*: `absolute_deck_elevation_m` when `OBJECT_MSL` fixtures supply it; otherwise
   datum-at-anchor (the anchor seat, as in feature A) plus `deck_top_y_m`.
2. *Deck ends in the solve*: abutment lines become pinned polylines using the tile-seam idiom
   (`split_pavement_at_seams` / `apply_seam_dem_anchors`): ring vertices are inserted exactly where
   pavement rings cross an abutment line and hard-pinned at deck elevation. The network grades up
   to them under existing edge budgets — no new solver machinery. Where no pavement ring crosses
   (pavement cut short of the abutment), the causeway approach is emitted analytically from the pin
   value using the existing approach-ramp emitters.
3. *Corridor*: `_emit_underpass_road_approaches` re-sourced — span footprint, clearance, and floor
   elevation from the `BridgeStructure`; road centerline from the DSF road network (W-R2) with
   OpenStreetMap fallback. Corridor floor = deck elevation − structure depth − clearance margin
   (KBNA: 161.0 m, confirmed by both the fixture pins and the anchor sample).
4. *Gate replacement*: `_scenery_has_bridge_objects` boolean grep retired in favor of classifier
   records (it currently misses all four EDDF crossings and suppresses our own deck emission
   wherever it does hit).

**Terrain-carried (EDDF class), generalized by R9.** The pavement drapes across the span and is
already in the solve. Two sub-cases by road state:

* *Road fully handled by the pack* (cosmetic trench, EDDF flush decks): the deliverable is
  restraint — the corridor/trench emitters must not fire under the deck (today an OpenStreetMap
  `bridge=yes` way beneath could trigger a carve that breaks the causeway). Classifier records
  suppress corridor emission inside terrain-carried deck footprints and log the suppression.
* *Road not (or only partially) lowered* (the EDDF elevated span; the terrain must RISE): a new
  law, `grade_law.bridge_crossing_floor`, puts a **floor band on the pavement nodes inside the
  span footprint**: floor = road surface elevation + `BRIDGE_ROAD_CLEARANCE_M` +
  `BRIDGE_DECK_STRUCTURE_THICKNESS_M` (structure depth from the object: deck top − ceiling).
  Per-node floors are first-class solver inputs (`node_band`), so **the hump solves itself**:
  the one-solve raises the crossing to the floor and the existing taxiway grade cap (1.5%) and
  vertical-curvature law shape the approach ramps on both sides — no ramp geometry is authored.
  Road surface elevation source, in order: the pack's own trench/cladding floor (partial
  depression, measured from the object) → the DSF road network's draped polyline over our solved
  corridor → the digital elevation model where the road is untouched. Where the object's own deck
  profile is sloped or crowned (KMCO/KDFW class), the deck-end profile values additionally pin
  the pavement at the span ends so the rendered object and the solved pavement meet exactly; the
  object's crest must sit at or above the solved crossing (audited, W-V).

**Profile-carried (A4; KMCO humps, EDDF A3 ramp).** Pavement is continuous across the span AND
the measured deck profile is non-flat: the object is the authority for the vertical shape and
the terrain is graded up to follow it. Handling: per-vertex profile pins along the span —
pavement nodes inside the deck footprint pinned to datum + profile(axis position), the exact
mechanism runway profiles already use (per-vertex `alt_abs` from birth). The crossing-floor law
still applies as a lower bound; approach grades outside the span solve normally (measured
approaches: 3.7–4.9% at KMCO, 9.4% on the EDDF hard ramp object — the ramp is drivable object
surface, so the solved terrain beneath tracks it from below, never above). Abutment ends that
reach grade are the anchor points; a structure failing the abutment-reaches-grade test at either
end is refused (piered viaduct, no terrain adaptation).

**Pack-data gap (A4; KDFW).** Aerosoft KDFW models neither deck geometry nor road depression —
the parkway is a DSF road network draped at grade and no bridge object exists. Nothing to
extract ⇒ feature B does not fire; the existing OpenStreetMap `bridge=yes` underpass machinery
(built on KDFW) remains the handler. This is the designed fallback, not a failure mode.

**Cosmetic decks** (`hard_deck = False`): treated as deck-carried for terrain purposes (the truck
routes ride the terrain, which must form the causeway and corridor), but the deck-end pin values
come from datum + deck geometry only — there are no fixtures.

### 3.3 Feature A — tunnels

1. *Datum / anchor seat*: the roofed body (roof footprint) enters layout as a **flat seat** at the
   anchor's solved elevation — the same mechanism as building pads (flat, heavy anchor), new role
   distinct from `ROLE_BUILDING` so validators can tell them apart. This fixes `terrain(anchor)` at
   layout time and breaks the circular datum (§2.4 supplies the reference values the result is
   audited against).
2. *Trench footprint* (amended by A1): the trench is the **full deck footprint** — body and
   mouths — not the mouths alone. The roof object is the at-grade surface over the body; terrain
   left at grade there would be coplanar with the roof slab and z-fight (§2.4). The trench
   polygon is carved out of any overlapping pads/boundary shapes at layout time (precedent:
   `_depressed_road_corridor_band` carving terminal pads), so nothing solves *over* the opening.
   Where the trench rim abuts solved pavement, those pavement nodes get a band ceiling at rim
   elevation. **Pavement overlap**: where auto_patch pavement itself crosses the body footprint,
   pavement value wins (R2) and the trench is carved only outside it — the resulting
   roof-slab-versus-pavement coplanarity is open question 5.
3. *Law*: `grade_law.tunnel_trench(datum, trench_polygon, deck_depth_samples) → rim ring +
   floor elevations`. Floor = deck depth **minus `TUNNEL_FLOOR_BELOW_OBJECT_DECK_M` (0.5)** —
   the mesh floor must sit strictly below the object's road deck, never at it (§2.4 point 3; the
   OBJ8 deck carries the visible road, the mesh only clears it). A flat pan per tunnel at the
   body's deck depth is lawful (validated in the wild); following the mouth ramp bins upward is
   permitted but optional. Rim = datum. Emitter and validator import this one function (R1).
4. *Emission*: last-block, skirt-style (after final grade projection and decimation, own
   `tile_cut`), emitting the floor polygon and node-split wall rows per R2. Rim welds to
   surrounding terrain; walls are the deliberate divergence.
5. *Interlock*: every consumed tunnel object lands on the R4 exclusion list; Phase 2 must not
   y-bake it. Negative-`OBJECT_AGL` tunnels (EGLL 6/7/10) are consumed with their offset applied to
   every depth (their geometry is authored above y = 0).

### 3.4 Feature C — split-level structure interfaces (measurement-gated)

**The evidence class (user, 2026-07-09):** airside and groundside commonly sit at different
grades with buildings between them, and the *object's construction* encodes the split — facade
bases at different local y on different sides. Three exemplar patterns: ELLX (terminal between
airside and a lower groundside: the landside base level says exactly where groundside sits);
LFPG (terminals abutting a central roadway lower than the aprons, the objects extending down
along it); LFLL (a railway station built around below-grade rail). The principle is the same as
features A/B: read the object for what the terrain must do, both to adapt the object to the
terrain and the terrain to the object.

**Extraction (as measured, A5):** the classifier records, per compass/radial sector of the
footprint boundary, the **low envelope** of facade bases — the minimum (≈ 5th percentile) solid
y of *wall columns* (vertical extent ≥ 2.5 m; roof-overhang and draped-decal edges otherwise
dominate as false levels), clustered at 0.5 m with a **minimum perimeter share (~5%)** per level
to drop basement/service parasites. A single level ⇒ the existing flat building pad (R5
unchanged). Multiple levels ⇒ piecewise-flat: one seat per interface level — R5 refined per this
user direction, not overturned.

**The A5 measurement verdict reframes the consumers.** The loud facade bands are DECOYS: at
ELLX the 5–10 m landside band is an *elevated departures roadway* (ramp objects rising 0 → 7.6
and 0 → 9.1 m from grade) and at LFPG the central access is an *elevated ring-road viaduct*
(wall base +4.27 m) — in both, ground-level geometry (y ≈ 0) co-exists on every side and the
correct terrain answer is **flat at grade**. The low envelope gets all three airports right; a
classifier trusting the dominant or highest interface would seat groundside several metres
wrong at two of them. Consumers, ordered by evidence:
1. *Groundside seating and the bowl-under-deck rule (ELLX versus LFPG T1, refined by A7).* The
   seat is the **low envelope** per side, and the deciding signal is the **ground-contact
   fraction**: when substantial y ≈ 0 geometry co-exists on a side (ELLX: both sides), terrain
   is flat and the vertical drama is object-carried — restraint with confidence. When a
   structure has essentially **no** ground-contact geometry (LFPG T1 drum: 0–23% per object),
   the **low envelope wins as a real bowl**, and a co-occurring elevated road deck *above* the
   same footprint (T1's 0 → +17.6 m helix) confirms bowl-under-deck rather than refuting it —
   the exact inverse of the ELLX decoy, separable only by the ground-contact test. Measured
   caveat: objects under-specify bowl *depth* (T1 shell base −3.4 m where the user's hand patch
   cuts −8 m) — the object gives existence, footprint, and a floor bound; depth margin, rim
   extent, and approach ramps are law/audit territory (the hand patch is the reference).
1b. *Interior cutouts (KDEN class, R10 — guards calibrated by A8).* Trigger: the structure
   contains drivable (`ATTR_hard_deck`/hard) triangles below effective grade that are ≥ 95%
   **enclosed** within its own at-grade plan footprint. Cut footprint = the enclosed below-grade
   hall (inset from the perimeter; perimeter ring stays at grade, node-split welded per R2).
   Floor = the below-grade **hard content's** minimum y minus a small clearance — keyed on the
   platform, never the deepest solid (KDEN carries non-hard foundation piles to −19 m; keying
   on deepest solid would blow the pocket floor 9 m past the platform — the interior-cutout
   analogue of A6's depth inversion). Guard (b) is perimeter-scoped: **never lower pavement
   nodes outside the structure footprint** — NOT "refuse when apt.dat covers the footprint"
   (KDEN's A/C platforms sit directly under apron concrete; the cut lives inside the building
   perimeter and the taxied pavement outside stays at grade). Enclosure is what separates this
   rule from the A6 tunnel rule (EGLL decks are open to the sky at the mouths; KDEN platforms
   are 100% enclosed), and the negative-`OBJECT_AGL` limb must NOT drive cutouts (at KDEN it
   catches only jetway slack and sunk signage — pure false positives here).
2. *Below-grade trench interfaces (LFLL class) — the surviving v1 scope.* The Lyon TGV station
   is cleanly bimodal: concourse at 0, rail trench floor at −10 m (min −10.9, box ≈ 121 × 773 m
   along the rail axis), with a −2 m mezzanine band to keep distinct. The −10 m depth exists
   ONLY in the object (the Roads-pack DSF network carries the alignment but is draped level-0,
   no metric depth). Treatment = feature A's trench machinery: floor from the object's trench
   interface, alignment from the DSF network / OpenStreetMap rail, walls largely provided by
   the station object (R8 spirit).

**Gate:** `O4_OBJECT_SPLIT_LEVEL_TERRAIN` (default off). v1 implements only the LFLL-class
below-grade trench interface plus the flat-confirmation low envelope; depressed-frontage seating
(the original ELLX/LFPG hypothesis) stays parked until an airport measures a real depressed
interface. Split-level structures whose terrain is adapted to them join the R4 exclusion list
exactly like tunnels. Anchor caution carries over from families A/B: these packs bake geometry
150 m–3.3 km from a single shared anchor, so the datum is one sampled terrain point.

### 3.5 Pipeline ordering (unchanged laws, new participants)

```
layout: placements + OBJ8 → classifier → seats, mouth carves, exclusion list
solve:  seats as flat anchors; abutment pins via seam idiom; rim ceilings
post-solve emitters (existing block): corridor + approaches (feature B)
…planarize → decimate (ordering law) → final grade projection…
last block (with runway-end skirts): mouth trenches + walls (feature A)
to_osm → Triangle4XP mesh → Phase 2 y-bake (minus exclusion list)
```

The known failure this respects: emitters that bake pavement reads before
projection/decimation get rewritten underneath (the reason skirts emit last). Feature B's coupling
is inside the solve (pins) so its corridor emission may stay in the existing block; feature A's
trench reads final values and therefore emits last.

---

## 4. Verification

* **Unit**: classifier fixtures (R6) — roofed-shell tunnel, hard-deck bridge, cosmetic bridge,
  terrain-carried flush deck, negative-`OBJECT_AGL` tunnel; contract thresholds; mouth extraction
  closure (every mouth polygon closed, area within tolerance of fixture truth).
* **Audit tools** (`tools/`, promoted per the persistent-tools rule):
  `object_terrain_feature_report.py` — per-pack classifier dump (counts, contracts, refusals);
  `tunnel_trench_audit.py` — mesh sample grid over each mouth versus law values, plus rendered
  deck-versus-floor delta at the datum; `bridge_deck_audit.py` — terrain at abutment pins versus
  deck elevation, corridor clearance profile along the road polyline.
* **Acceptance numbers**:
  KBNA taxiway L — abutment terrain = 167.0 m ± 0.25, corridor ≤ 161.0 m + 0.25 under girders,
  road polyline continuous through the span;
  EGLL tunnel 2 — trench floor continuous over the body at datum − 5.0 − 0.5 m ± 0.25 (strictly
  below the object deck), rim at datum, walls near-vertical (node-split), zero trench triangles
  outside the deck footprint; audit reports the datum delta versus the author values (21 m for
  tunnels 2/9/10/11, 28 m for 6/7);
  EDDF taxiway P — grade continuous across both decks (no corridor fired), existing suite green.
* **Mesh-quality gate**: zero near-parallel pairs (chain-identity gate) on every touched tile;
  triangle-count delta reported per tile.
* Both feature flags stay **default off** until the three-pack audits are green and the user flips
  them (lockstep with their validators, per project convention).

---

## 5. Workstreams and ownership

No two workstreams edit the same file. The supervisor (main session) reviews every diff, runs the
suite, and commits; agents never run `git add`/`git commit`.

| id | deliverable | owns (exclusive) | depends on |
|---|---|---|---|
| W-R1 | `OBJECT_MSL` + negative-`OBJECT_AGL` placement reading, tools parity | `src/auto_patch/obj8_reader.py`, `tools/obj8_geometry.py`, `tests/test_obj8_reader_placements.py` | — |
| W-R2 | DSF road-network reader (segments, shape points, level flags) | `src/auto_patch/dsf_road_network.py` (new), `tests/test_dsf_road_network.py` | — |
| W-R3 | classifier | `src/auto_patch/object_terrain_features.py` (new), `tests/test_object_terrain_features.py` | W-R1 interface |
| W-B | bridge integration (pins, corridor re-source, gate replacement, suppression) | `src/auto_patch/bridges.py`, feature-B seams in `pipeline.py`/`clearance.py` | W-R2, W-R3 |
| W-T | tunnel integration (seat, carve, law, last-block emission, R4 interlock) | `grade_law.py` (new function), feature-A seams in `pipeline.py`/`post_mesh.py`, emission module | W-R3, W-B's pipeline edits merged |
| W-V | audit tools + acceptance runs on the three packs | `tools/object_terrain_feature_report.py`, `tools/tunnel_trench_audit.py`, `tools/bridge_deck_audit.py` | all above |

Execution order: W-R1 ∥ W-R2 (disjoint files) → W-R3 → W-B → W-T → W-V, with supervisor review
between every step. `config.py` and `pipeline.py` are supervisor-mediated: agents propose exact
patches for those files in their reports rather than editing them concurrently.

---

## 6. Open questions (not blocking the readers)

1. **Datum source when an author mesh exists** (§2.4): do we match the author's flatten elevation
   at EGLL, or our own solved terrain? Defaulting to our solved value with the audit reporting the
   delta against the author mesh; user ruling wanted before flag-on.
2. Whether the corridor clearance margin under deck-carried bridges needs a law of its own or
   inherits the existing underpass constants (KDFW rulings).
3. EDDF cosmetic swale: wanted at all?
4. How far causeway approaches may run before yielding to the adjacent-ground law (interaction
   with the corridor law's enforce-fully ruling).
5. ~~Pavement crossing a tunnel body~~ **Resolved by R8** (user, 2026-07-09): hard deck ⇒ cut
   our pavement over the object footprint, the object sits flush and carries the surface;
   non-hard ⇒ pavement wins, no cut.
6. `BRIDGE_ROAD_CLEARANCE_M` default for the crossing-floor law where the object's own geometry
   does not pin it (a real road corridor wants ≥ 4.5–5.1 m; the KDFW underpass rulings may
   already answer this — W-B checks before inventing a constant).

---

## 10. Amendments

**A1 (2026-07-09, author-mesh dissection).** §2.4 filled with measured results. Three design
changes over the original draft: (i) the feature-A trench is the **whole deck footprint**, not
the mouths only — the roof object is the ground over the body, and terrain at grade there would
z-fight the roof slab; (ii) the trench floor law gains `TUNNEL_FLOOR_BELOW_OBJECT_DECK_M` (0.5 m)
— the mesh floor sits strictly below the OBJ8 road deck, which carries the visible road; (iii) a
flat pan per tunnel is lawful; the ramp-profile floor is optional. The no-holes verdict is
confirmed from a second independent direction (the author, with full freedom, also built walled
trenches). New open question 5 (pavement crossing a tunnel body). The classifier's mouth
polygons remain in the record — validators use them (rim walls at mouths are the visually
exposed ones) even though the emitter cuts the full footprint.

**A2 (2026-07-09, user rulings R8/R9 + generalized vertical model).** Open question 5 resolved
by R8 (hard-deck flush seating: pavement cut over hard tunnel roofs and deck-carried spans).
§3.1 `BridgeStructure` gains `deck_top_profile` / `deck_end_elevations_y_m` (a single deck plane
cannot represent the KMCO/KDFW rising-bridge class). §3.2 terrain-carried contract split into
road-handled (restraint) and road-not-lowered (`bridge_crossing_floor` law: per-node floor =
road + clearance + structure thickness; the hump emerges from the one-solve under the existing
grade and curvature caps). Measurement pass over EDDF's elevated span, KMCO, and KDFW launched
to validate the profile model before W-B consumes it.

**A3 (2026-07-09, user).** Feature C added (§3.4): split-level structure interfaces as elevation
evidence — ELLX (groundside seating from the landside facade base), LFPG (central roadway lower
than airside), LFLL (below-grade rail into a station). Classifier record
`StructureGroundInterface` added; R5 refined to piecewise-flat (one flat seat per interface
level) for split-level structures. Implementation is measurement-gated: an ELLX/LFPG/LFLL
dissection pass must first prove the perimeter-base-profile signal is mechanically readable and
define the noise filters. Pipeline-ordering section renumbered §3.4 → §3.5.

**A4 (2026-07-09, EDDF/KMCO/KDFW measurement pass).** The longitudinal-profile model (A2) is
confirmed *required*: measured decks span flat (EDDF Bridge_2/3/4, −0.02), single-slope ramp
(EDDF A3 east: hard ramp 0 → +5.96 over 63 m to a +6.5 deck, road at grade beneath), and crowned
hump (KMCO puente/puente2: ends +0.7…+1.7, crests +5.15/+5.18, pavement 60–63% continuous).
Four corrections: (i) new contract **PROFILE_CARRIED** — continuous pavement with a non-flat
deck; flat-across handling would erase the KMCO humps; handled as per-vertex profile pins, the
runway-profile mechanism; (ii) deck detection accepts near-horizontal **`ATTR_hard`** planes,
not `ATTR_hard_deck` only (every humped bridge measured carries plain `ATTR_hard`); (iii)
**abutment-reaches-grade test** added to exclude piered viaducts (KMCO via_tren, min y +3.45,
would otherwise induce a false +4 m causeway); (iv) KDFW encodes nothing vertically (road net
draped at grade, no deck object) — feature B correctly does not fire and the existing
OpenStreetMap underpass machinery remains the handler. Also measured: no `OBJECT_MSL`/AGL pins
exist outside KBNA — datum-at-anchor plus local profile is the normal absolute-elevation path.

**A5 (2026-07-09, ELLX/LFPG/LFLL measurement pass).** The split-level facade-base hypothesis
**fails at ELLX and LFPG and survives at LFLL**, and §3.4 is reframed accordingly. ELLX: both
sides of the terminal reach y ≈ 0; the apparent landside drop is an elevated departures-roadway
object family — terrain is flat. LFPG: the central roadway is an elevated viaduct (+4.27 m),
the inverse of the hypothesis; the −3 m drum ring is a foundation moat (parasite). LFLL: clean
bimodal signal — concourse 0 / rail-trench floor −10 m over a 121 × 773 m box; the depth exists
only in the object (the Roads DSF network is draped, depth-free). Filters that made the signal
readable, now normative in §3.4: wall-column extraction (≥ 2.5 m vertical), low-envelope per
sector (never dominant/max), ≥ ~5% perimeter share per level, and mandatory
structure-versus-terrain disambiguation (co-existing ground-contact geometry ⇒ flat). No
terminal measured carries any hard surface, and none is `OBJECT_MSL`-pinned. Feature C v1 scope
narrowed to the LFLL-class trench + flat-confirmation; depressed-frontage seating parked.

**A6 (2026-07-09, EGLL below-grade-versus-author-mesh correlation).** The author's mesh is the
buried/exposed oracle, and it rules: **every below-grade building piece is buried** (30/30
resources, deepest −9.22 m — mesh flat at datum over all of them); only drivable tunnel decks
are exposed. Feature A's detection signature is replaced (§2.1): drivable surface reaching
below grade, or `OBJECT_AGL` ≤ −1 m — **not depth**, which is exactly inverted (a depth
threshold would flag ~200 buried false positives and miss shallow tunnel 4). Tunnels are
mesh-walled; buildings carry their own buried walls under a flat mesh. Feature C consequence:
below-grade building geometry is never intended-visible at EGLL — buried-by-default is the
correct classifier posture, with the LFLL rail trench reachable only through its own drivable/
station-interface evidence, not through depth. §2.4 gains point 5; W-R3 round 2 received the
hardness requirement and the −1 m `OBJECT_AGL` threshold as a live addendum.

**A7 (2026-07-09, LFPG hand-patch correlation; user correction of A5).** The user's hand-built
patch (1,874 nodes / 134 ways; `altitude` flat polygons + `altitude_high/low` ramp quads per
`O4_Vector_Map.include_patches`) encodes 2.18 M m² of deliberate depression below the 103 m
field: the **T1 bowl** (flat = 96, 65,738 m², 7–9 m below field/frontage — A5's "foundation
moat parasite" reading was WRONG; the −3.43 m drum base is the real sub-grade floor and the
"+4.27 viaduct" is the low reach of a 0 → +17.6 m helical road) and the **T2 central spine**
(flat = 96/97 corridors, deepest 94, threading the 103 m halls). Object evidence recovers the
T2 spine **cleanly** (23 objects form one continuous −7.5 m below-grade level — rail trench at
−7.56 plus basements to −9.22 — over a 3 km footprint matching the patch corridors: patch −7 m
versus objects −7.5 m) and recovers the T1 bowl **partially** (existence/footprint yes; depth
under-specified by ~4.6 m). New normative rule in §3.4.1: ground-contact fraction decides
between flat-with-object-drama (ELLX) and bowl-under-deck (T1); the A5 perimeter-share filter
must not kill a below-grade level carried by the *dominant-area* structure. Datum note: the
patch rebases the field ~20 m below raw SRTM — object↔patch comparisons are differential only,
reinforcing open question 1.

**A8 (2026-07-09, KDEN measurement pass — R10 guards calibrated).** KDEN's terminal and
concourses are **`.agp` autogen tiles** (55–110 part objects at one shared anchor each; the
`agp_reader` path matters — the classifier feed must include `.agp` part objects, not just DSF
`OBJECT` rows). The AGTS station halls are baked into the concourse parts: `ATTR_hard_deck`
platform floors at −5.0 m (A: ~580 m², B: ~800 m²) and −4.9 to −9.9 m (C), **100% enclosed**
within the at-grade footprints; 24 escalators are `OBJECT_MSL`-pinned down to the platforms, so
they only look right if the mesh is actually dropped. Verdict: **the A6 tunnel rule and the
interior-cutout rule are two rules** — the same drivable-below-grade signature fires on both,
and *enclosure* is the discriminator. §3.4.2b is the normative statement. Honest cases on
record: Jeppesen's station is essentially unmodeled (16 m² of platform) while its escalators
still descend — a W-V audit case, since no rule can manufacture a hall the pack doesn't model;
the inter-concourse AGTS tunnels are not modeled at all (nothing to cut between buildings —
correct no-op); jetway slack (`OBJECT_AGL` −3.77) is excluded by both hardness-below-grade and
enclosure gates.

**A9 (2026-07-09, classifier round 4 — Feature C implemented, calibrations recorded).**
`StructureGroundInterface` landed (128 tests): sector low-envelope extraction, four interface
classes, enclosure precedence ahead of the tunnel branch, non-flat interfaces feeding R4
exclusions. Measured deviations adopted over the work-order letter: (i) A7's "dominant-area
object bases below grade" is unusable literally (mega-bake dominant objects have elevated
median bases — rooftop columns); the working pair is ground-contact fraction ≈ 0 AND at-grade
wall-base share ≤ 0.10 (measured: T1 0.05 versus T2A spine 0.32 versus EGLL buried ≈ 0.5);
(ii) trench footprint-area gate 1,000 m² (EGLL buried-basement group 48 m² versus smallest true
trench 8,885 m²); (iii) a building-likeness gate (≥ 500 wall columns) ahead of the bridge path
only (terminals 1,968–13,511 columns versus true bridges 0–169 — the ELLX departures ramp was
being swallowed by the viaduct guard). Correction: the LFPG −7.5 m spine family ships in the
TAIMODELS pack (pack 2), not pack 1. **Open question 7 (user ruling wanted):** ELLX SkyPark's
underground garage measures 93% enclosed — between EGLL mouths (~50%) and KDEN halls (100%);
at the calibrated 0.95 threshold it classifies TUNNEL, at 0.90 INTERIOR_CUTOUT. Kept at 0.95
for v1; both sides of the band now carry real data. **W-T note:** the negative-`OBJECT_AGL`
tunnel limb needs a minimum-footprint guard before consumption (KDEN jetway slack would
otherwise produce false tunnel records — pinned in the tests with a flag comment).

**A10 (2026-07-09, KBNA three-way calibration — author mesh | hand patch | object model).**
The GloballArt author mesh is the aligned reference (user-confirmed, measured): the
object-derived values match it **exactly** at every site — taxiway-L abutments 167.0 both ends,
corridor floor 161.0, depth 6.0 m, girder clearance 4.2 m — while the user's hand patch runs
+3 to +10 m high at all four crossings (independent DEM-faithful treatment predating the pack;
its 172 m causeway would bury the fixture-pinned deck). No case exists where both references
agree against the object model. Calibrations now normative for feature B:
(i) **corridor floor = absolute deck elevation − hard-deck height** (geometry-driven; the
clearance constant is a *check*, not the driver — a clearance-driven floor over-digs by ~0.9 m
here); (ii) `BRIDGE_ROAD_CLEARANCE_M`: the validated in-the-wild minimum is **4.2 m**; 5.1 m is
retained only for the crossing-floor law where we choose the vertical split (EDDF class), and
the deck-carried corridor *validator* accepts ≥ 4.2; (iii) **cosmetic decks get the identical
causeway-plus-corridor treatment as hard decks** (author: same 167/161/6.0 at both Murfreesboro
bridges); (iv) **no taxiway-side terrain ramp** — causeway flat at datum to the abutment lip,
near-vertical across-road walls (node-split, R2), along-road grades 1.5–3%, depressed road
extends ≥ 240 m per side (open question 4 datum); (v) the author hand-flattens the field core
at exactly 167 m (67% of samples), the EGLL 21 m pattern repeated — open question 1 gains a
second datum point. Oracle validated at a second airport: the structure pass found all bridge
corridors EXPOSED with exact floors, and the first end-to-end `--depressions` run scored 100%
recall on the four true corridors but ~5% precision on contoured terrain (creek valleys read as
interior depressions; the enclosure claim is untested; object association is bounding-box) —
needs a real enclosure test and footprint intersection before auto-confirmation. Classifier
worklist from this pass: taxiway-L itself returns AMBIGUOUS under standalone drape-coverage
evidence (the flagship exemplar refuses; the in-pipeline solved-pavement union is the intended
evidence — expose the coverage fraction on the record so tools can print it); Crossing_Bridge
is silently absent from classification (suspect the 50-placement clutter filter or footprint
gates — must at least surface as a refusal); plus the A9/W-V mega-pool defect.

**R12 — Geometry at layout, elevation from the solver, nothing moves after (user, 2026-07-09).**
Minimize mutation passes. Feature-B (and later feature-A/C) terrain shapes are BORN at the
geometry phase as first-class layout shapes with their own roles (bridge trench, bridge
causeway), the R8 pavement cut happens when shapes are built, and the solver owns all elevation
(trench per-vertex alt_abs from birth — the runway precedent; causeway pinned at the deck-end
law value). Post-solve plate emission and deconflict participation are retired for these
features. Supersedes the iteration-3 post-solve cut/plate approach; W-B iteration 4 implements
the restructure. Amendment recorded live during the KBNA build iterations (J/R abutments pass
at 167.10/167.09; the trench plate's repeated loss to mutation passes is what this ruling ends).

**R13 — An open pit takes the pavement with it (owner, 2026-07-30).** Ruling, verbatim: *"for
below grade drainage objects, cut a trench in the pavement."* **Our pavement is cut over the
footprint of a modelled OPEN PIT**, exactly as R8 already cuts it over a hard deck — through the
same `bridges.cut_pavement_over_footprint`. This is a deliberate amendment to R2 ("pavement value
always wins at any contact") and an extension of R8 past its hard-deck condition: an open pit
provides no deck at all, so unlike R8 nothing is left carrying a drivable surface there. That is
the point — the hole is real, and pavement spanning it is the defect.

*Why it was needed.* The two OTHH basins the owner reported were buried by exactly this
interlock. Both lie wholly under an auto_patch `apron`, so the trench floor yielded to the last
square metre (Drainage_04 2 054 of 2 055 m², Drainage_05 all 519 m²) and no floor plate was ever
born. The apron was checked against the source and is FAITHFUL, not an over-reach: the pack's own
DSF draws `Ground/Poly/ASPH3.pol` straight across both pits, and `pav_polys` is the union of
apt.dat row-110 and DSF draped pavement. (apt.dat alone — the pack's file and Global Airports
alike — leaves the area unpaved: both points sit 22-30 m OUTSIDE the row-110 union, and outside
it entirely rather than in a hole. The two sources genuinely disagree, and taking the union is by
design.) So the pavement is really there, and only a cut can expose the pit.

*Scope — the narrow predicate.* R13 fires on `object_terrain_features.is_open_pit_interface`:
a carved basin (R4's `is_carved_basin_interface`) that is a `BOWL_UNDER_DECK` admitted by the
bowl rule's OPEN-PIT limb (`above_grade_area_fraction <= BOWL_MAX_ABOVE_GRADE_AREA_FRACTION` —
"essentially nothing above grade IS the pit signal"). Every other carved class keeps R2, because
something the pack authored is the visible surface there: a `BOWL_UNDER_DECK` admitted by the A7
ground-contact limb (LFPG T1's drum over its sunken floor), and `TRENCH_SPINE` (LFPG T2's halls
at grade over one continuous level; at OTHH the two Dewatering pits pooled with their aux
buildings and control posts). `INTERIOR_CUTOUT` is unaffected — R10 forbids carving pavement
outright.

*Reach — airside AND landside (owner, 2026-07-30, second directive: "chase any additional objects
that are below terrain, they should be getting carve outs").* R13's cut covers LANDSIDE pavement
(`ROLE_GROUNDSIDE_PAVEMENT`) as well as the airside/service roles R8 uses. A hard deck seats flush
inside the airside network, but an open pit can sit anywhere — measured at OTHH, two of the six
basins are buried by groundside pavement with *zero* apron over them (Drainage_02 100 % of its
body, Drainage_06 4 155 of 5 121 m²), so an airside-only cut left them buried. `bridges.
pavement_cut_roles(include_groundside=...)` is the one place the two scopes are written down;
R8's is deliberately unchanged.

*Ordering (load-bearing).* The cut runs BEFORE the anchor-seat pin, not merely before the floor
geometry. The seat only fires where no earlier shape owns the anchor, so with the apron still in
place it declines — and the object then drapes on our own trench floor and sinks by the cut depth,
which is exactly the "object sitting below terrain" defect the seat was added for (2026-07-18f).
Both OTHH targets anchor inside their own pit body, so both were exposed; with the correct order
they are seated at datum 1.27 m and 1.13 m.

*Guard.* A cut that then seats no trench floor is PUT BACK (facility-scoped, so an anchor seat
born after the cut is undone with it) and the restore is logged. Pavement removed with no trench
under it is a hole in the drivable surface — strictly worse than the buried pit it was meant to
expose.

**R14 — Everything more than 1 m below grade is cut out (owner, 2026-07-31). IMPLEMENTATION
PENDING.** Ruling, verbatim: *"for any object that extends more than 1m below grade, I want cutouts
so the whole below grade portion is visible."*

This is a general law, and it **supersedes amendment A6's buried-by-default** for anything past the
1 m line: below-grade slack is no longer written off as invisible foundation depth. It also makes
ruling **R10's interior cutout a requirement rather than a design note** — R10 already specifies the
shape (carve inside the at-grade footprint, down past the below-grade content) but has never had an
emitter, and its guards were parked pending a KDEN calibration pass. R14 is the instruction to
build it.

*Scope not yet fixed — one input outstanding.* BRIDGE-family structures are held back: the owner
asked for a KML of the bridges that get no cutout today in order to rule on what they should do
(`tools/probes_othh_basins_20260730/OTHH_bridges_no_cutout.kml`; at OTHH, Bridge_02 −9.67 m,
Bridge_03 −8.72 m, Bridge_05 −5.32 m and Bridge_06 −1.99 m have none, while Bridge_01 and
Bridge_04 are cut as tunnels). That ruling decides whether R14 reads "cut every below-grade solid"
or "cut every below-grade solid that no bridge/tunnel feature already owns", which is a different
emitter, so the build waits for it.

*What is already measured* (the classifier's own interface records, not a re-pooled approximation):
at OTHH six pools are at-grade structures with a genuine below-grade interior — Qatar Duty Free
−12.21 m, TerminalRoads_Parking −9.09 m (an underground car park), Fuel_02, FuelFarm_01,
GA_Hangar9, HangarC — with wall-base-at-grade 0.29–0.77, i.e. their walls do meet the ground. Those
are the R10 cases R14 turns on. Three further resources carry below-grade geometry with no
clustered below-grade level (the A6 class R14 now claims).

*MEASUREMENT LAW — "below grade" is not the object's own negative y (owner correction,
2026-07-31).* An object drapes at `terrain(anchor)`, so authored `y = 0` is the ground at ONE point.
Reading negative authored y as "buried" therefore counts every pier, abutment and foundation stub
of a perfectly above-ground structure. Measured on the six OTHH bridges — all of which the owner
confirms are above-ground road bridges — every deck END sits within ±1.9 m of the datum
(+0.77/−0.55, +1.90/−0.39, +0.23/+0.24, +1.08/+1.09, +0.78/−0.70, −1.22/−1.28) while the lowest
face reaches −7.60 m; on Bridge_02 the −8..−2 m bands hold 305 m² of faces carrying **zero** deck
area, and Bridge_03 the same (328 m², zero deck). R14's 1 m test must therefore be applied to
structure that is genuinely below the LOCAL ground — deck/floor surfaces, not foundation stubs —
or the emitter will carve a pit under every bridge pier on the field.

*Corollary — the deck-end contract (owner, 2026-07-31).* For a bridge or ramp the wanted outcome is
not a cutout at all: *"they just need to be set so their top edge (the road deck) at either end is
flush with grade."* Terrain meets the deck; where the deck runs below the surrounding ground the
terrain is graded UP to it, not dug away beneath it. Bridge_01 is the live counter-example: it
carries 1 113 m² of DECK-like face more than 1 m below datum (a ramp descending to the terminal's
below-grade level, reaching −5.84 m), is classified a TUNNEL today, and its trench cut to −6.34 m
puts terrain *below* the ramp and leaves it floating — the defect the owner reports. Bridge_04 is
the same treatment applied to an essentially flat deck (ends +0.23/+0.24, only 8 m² of deck below
−1 m).

*W1 as BUILT — the AGL limb's two above-grade gates (2026-07-31).* A structure reaches the tunnel
classifier's AGL limb only if it looks like a shell living below the grade plane.
`TUNNEL_AGL_MAX_ABOVE_GRADE_HEIGHT_M` (2.0 m) catches a structure that TOWERS over grade, and W1a
made it — and the below-grade deck-area floor — read the WHOLE structure rather than the seeding
resource, which retired Bridge_01's wrong trench. It cannot catch a LOW bridge: Bridge_04 crests at
+1.91 m, inside the cap, and the underside of its at-grade slab reads as below-grade deck (1,022 m²
of near-horizontal face between −0.5 and −1.0 m; only 8.4 m² below −1.0 — slab thickness, not a
floor). The second gate, `TUNNEL_AGL_MAX_ABOVE_GRADE_DECK_AREA_M2`, refuses any structure carrying a
deck's worth of near-horizontal area standing clear above grade (≥ `+TUNNEL_ROOF_TOP_TOLERANCE_M`),
because a below-grade shell does not have a deck on top of grade and a bridge does. Measured on the
installed packs:

| structure | area ≥ +0.5 m | area ≤ −1.0 m | outcome |
|---|---|---|---|
| OTHH Bridge_04 | 1 650.6 m² | 8.4 m² | refused (8× over the floor) |
| EGLL Tunnel/7 | 0.0 m² | 52.8 m² | still a tunnel — the only real AGL-limb case |
| EGLL Tunnel/6 | 0.0 m² | 50.0 m² | unchanged |
| EGLL Tunnel/10 | 128.7 m² | 55.1 m² | kept, 1.55× under the floor |

The alternative the plan proposed — deepening the below-grade floor to `TUNNEL_MIN_BODY_DEPTH_M`
(2.0 m) — was measured and REJECTED: EGLL Tunnel/7 falls to 19.4 m², under the 25 m² gate, and stops
being a tunnel. Tunnel/10's 128.7 m² is exactly the figure the height cap's own comment cites when
it warns that "an above-grade AREA test cannot do this job"; that warning is about a FRACTION test
(Tunnel/10 carries more near-horizontal area above grade than below) and it stands — an ABSOLUTE
floor is a different test and clears Tunnel/10 with margin.

*⚠ W1b's premise does not hold at OTHH — measured 2026-07-31, BEFORE building it.* The plan's W1b
adds a deck-flush partition outcome so a bridge that today takes no pins gets its two deck ends
pinned. It cannot reach any OTHH bridge, because **none of the six ever becomes a `BridgeStructure`**
— all six carry ZERO hard triangles (hardness codes all 0), so `_hard_face_components` seeds nothing
and only the whole-pool COSMETIC limb can fire. Three distinct gates stop them:

| bridge | pool | n | gate |
|---|---|---|---|
| Bridge_01 | #1 | 2 251 | building gate — 57 299 wall columns ≥ `BUILDING_MIN_WALL_COLUMN_COUNT` (500) |
| Bridge_02 / _03 / _06 | #65 | 239 | merged into ONE candidate, refused *"piered viaduct: no solid geometry reaches effective grade within 35 m of the deck end(s)"* |
| Bridge_04 | #5495 | 5 | cosmetic limb needs ≥ `BRIDGE_MIN_DECK_AREA_M2` (200 m²) of deck at or above `BRIDGE_DECK_CARRIED_MIN_HEIGHT_M` (+2.0 m); it has **0.0 m²** |
| Bridge_05 | #5494 | 5 | same floor; it has 45.7 m² |

So the deck-flush partition is an EGLL-only change as specified (4.obj: `DECK_CARRIED`, hard_deck,
crest +7.84, ends 7.84/7.84, 52 m, zero deck-end pins today). Making it reach the owner's bridges
needs a prior ruling on which of the three gates yields — the cosmetic limb's +2 m elevated-deck
floor, the mega-pool building gate, or the pooling that merges Bridge_02/03/06 — each with its own
blast radius across every pack. Recorded, not decided.

*Known implementation hazard, recorded so it is not rediscovered.* The plan extent of a below-grade
portion cannot be taken from the frame's triangle list alone: a perfectly vertical pier, abutment or
shaft face has zero horizontal area and is dropped from that list, so a triangle-only union draws
nothing for a wall-only structure (measured on Bridge_04 and Bridge_05, both empty). Nor from the
raw vertex columns alone: those are sparse over a large flat slab whose interior carries no
vertices (Bridge_02 collapsed 4 058 m² → 87 m²). The union of the two is what the KML uses and what
the emitter will need.

---

## 11. Status — Feature A build (W-T, 2026-07-18)

**Feature A (tunnels) is BUILT, DEFAULT OFF (`O4_OBJECT_TUNNEL_TERRAIN`, default `"0"`).**
Files: `config.py` (gate + `TUNNEL_FLOOR_BELOW_OBJECT_DECK_M = 0.5`), `grade_law.py`
(`tunnel_trench_floor_elevation_m` / `tunnel_trench_rim_elevation_m`, the lockstep single
source), `bridges.py` (the flat-plate birth primitive `_born_flat` promoted to module-level
`born_flat_solver_plate` with a `record_pins` opt-out; no other change),
`object_terrain_assembly.build_tunnel_layout_shapes` (the emitter, consuming
`classification.tunnels` beside the bridge emitter), `layout.py` (`ROLE_TUNNEL_TRENCH`),
`pipeline.py` (one wiring call). Classifier, R4 exclusions, and y-bake drops shipped earlier.

**Design as built (per §3.3 + amendment A1, ruling R12).** One WHOLE-BODY trench per tunnel,
born pre-solve at layout time from the classified deck footprint: a flat floor pan at
`datum − body_depth − 0.5 m` inset `_TRENCH_INSET_M`, and a rim collar at the datum, the two a
node-split gap apart forming the R2 near-vertical wall. Datum = DEM at the placement anchor
(circular-datum rule); the negative-`OBJECT_AGL` offset is already folded into the classifier's
effective `body_depth_m`, so it is not re-applied. PAVEMENT WINS (R2/R8): the airside pavement
union is subtracted from the body before birth and the yielded area is logged per tunnel.

**One deliberate deviation from the literal work order, with data.** The order said the trench
role must be in the solver's `PAVEMENT_ROLES` "exactly like `ROLE_BRIDGE_TRENCH`". Measured, that
reused role dragged **30 % of EGLL airside pavement down, up to 8.3 m near tunnels** (the deep
floor pins couple neighbouring pavement through the one-solve — correct for a bridge deck-end,
wrong for off-pavement tunnel terrain, §2.5). `ROLE_TUNNEL_TRENCH` is therefore a LAW-tier
off-pavement terrain role (decimation-exempt, per-node `alt_abs`, LAW-tier weld — all the
`ROLE_BRIDGE_TRENCH` plumbing) that is **NOT** a pavement solver member and births with
`record_pins=False`. This decoupling also keeps the change inside the W-T file scope (no
`solver_primitives.py` edit). Re-measured: airside pavement mean move **0.004 m**, max 0.94 m,
99th pct 0.05 m; far-field (>60 m) 0.3 % moved by ≤0.05 m, zero >0.5 m. Floors still land in the
mesh (flat-by-law per-node `alt_abs`, not solver membership — layout.py `force_per_node`).

**Acceptance measured.** EGLL production mesh vs the author-mesh oracle (§2.4, ruling R11): the
large well-formed tunnels match within ±1.0 m of the author's cut (tunnel 2 depth 5.5 m vs 6.0;
11: 6.4 vs 7.0; 8: 4.5 vs 5.0). Where they diverge (tunnels 9/12) the OBJECT's own `body_depth`
under-specifies the depth the author cut — the object-vs-author disagreement open question 1 was
raised to quantify, not an implementation error (we render each tunnel to its object's depth and
the audit reports the delta). Tunnel 5 is fully under airside pavement ⇒ correctly NOT cut (R2,
open question 5). EGGW (0 tunnels) patch byte-identical gate-on vs gate-off (provenance header
excepted). CYYZ: the road tunnels-under-taxiways classify as BRIDGES (Feature B); the one
Feature-A tunnel is a Terminal-1 below-grade part (`Parte49`, 3656 m², cut to depth 2.46 m).

**Before the gate can default ON (owner review).** (i) Rule on the object-vs-author datum/depth
disagreement (open question 1) — do we deepen shallow-object tunnels toward the author, or hold
the object? (ii) Ship the `verification.py` lockstep validator that imports the `grade_law`
tunnel functions (out of W-T file scope this round). (iii) Rule whether an enclosed terminal
below-grade part (CYYZ `Parte49`) should be Feature A (open trench) or Feature C (interior
cutout, R10). (iv) Add `ROLE_TUNNEL_TRENCH` to the flat-airport fast-path REFUSAL set
(`flat_airport_fast_path._flat_refusal_reason` `bridge_roles`, out of W-T scope): a flat airport
whose ONLY relief is a tunnel would otherwise take the fast path and skip the trench. Latent only
— EGLL/CYYZ refuse the fast path for other reasons (crossing zone / object-bridge pins), so no
test airport is affected, but it must land before a flat tunnel-only airport is built gate-on.
