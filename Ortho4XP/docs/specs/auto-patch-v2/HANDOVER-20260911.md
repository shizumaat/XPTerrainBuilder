# Auto-patch v2 handover — 2026-09-11 (end of the object-stage switch session)

Written by the Fable 5.1 project-manager session for its successor. Canonical
record: `Ortho4XP/docs/RULINGS.md` (entries 2026-09-09ad … 2026-09-11am this
session). Specs: `Ortho4XP/docs/specs/auto-patch-v2/` (Fable writes ALL specs).
Memory: `~/.claude/projects/-Users-noah-XPTerrainBuilder/memory/` —
`session-20260903b-wall-crest-dem.md` (read its tail first), `MEMORY.md` index.

## 1. Where things stand

* **Main** `0ac1e695` = **App 1.0.319** (engine 1.50.1765), built and committed.
  v2 twin set green: 1,097 passed, 1 skipped (`cd Ortho4XP && venv/bin/python -m
  pytest -q --no-header -p no:cacheprovider tests/auto_patch_v2 tests/test_harness.py`).
* **No lanes running.** Every lane branch of the session is merged.
* **The campaign goal** (owner, RULINGS 2026-09-09b(6)): terrain and objects that
  read as plausible to a pilot taxiing in the sim. The OWNER'S SIM READ is the
  acceptance; census numbers are the report beside it.
* **THE OBJECT-STAGE SWITCH is adjudicated** (owner 11ac: "LEMD with 316 looks
  great"). The seat (v1's vertex rewrite of `.obj` files) is replaced by:
  MSL→AGL placements in the DSF (stock too), rigid bodies split into their own
  object files with anchors where the terrain equals their zero, X-Plane drapes
  each; `[rebake] placement = "agl"` (default) | `"seat"` still reachable by the
  key until §8 retires it.

## 2. The laws that govern the object stage now (spec `object-placement-spec.md`)

| § | law (one line) |
|---|---|
| §3, §12 | DSF dump→edit→encode→verify→write with `.dsf.anchor_bak`; EVERY read resolves the PRISTINE DSF (`dsf_write.pristine_dsf_path`); dump cache keyed by CONTENT hash; restore removes the previous write's `body_files` — idempotent over a written pack |
| §9, §10 | coarsening by `split_tol_m` 0.3; restore `.obj.anchor_bak` first; one-component line objects segmented by station |
| §13 | an ELEVATED body (lowest vertex > `elevated_base_m` 0.5 above the object's zero) NEVER has a file of its own |
| §14 | FOOTLESS bodies are CARRIED (the carrier's anchor + offset); a BASIN is ONE file anchored at a RIM point (interior of its own ring excluded from the search) |
| §15 | STANDS-OVER is the carrier (plan overlap > contact > distance, across the whole UNIT); duplicate rows are one placement; off-sheet anchors are never evidence |
| §16, §16a | NO thickness gate under `agl` (every OBJECT row is in the population); a CARRIER IS A SOLID (`carrier_fill_min` 0.2, hull overlap); a carried body is cut WHERE ITS CARRIER IS CUT and judged by `zero − zero_beneath` (0.3); a footed body whose own feet are off the surface is re-cut by its feet; basins exempt from the carrier ground test |
| §11–§11b | canopy/colonnade: body is the unit; FOOT ROWS (all-or-nothing per body, neighbour-pair feasibility, priced at `pad_flat`) — thread CLOSED (11ad), residue = mesh density under bodies, parked |

Design surface laws added this session (`design-surface-spec.md`): §20 pads take
the apron edge level; §21 runway trend; §22 skirted buildings need no pad; §23
ground datum; **§24 the basin's cut ring hugs the object's wall face, floor = plate
− `basin.floor_clearance_m` 0.5**. Classification (11af): `[lot]
apron_cover_fraction` 0.1 — a page ≥ 10 % covered by OSM apron is never a car park;
the landside demotion yields to stand evidence.

## 3. Current census (LEMD, plan replay on the 1.0.317 frame; OTHH dry run)

| measure | LEMD | OTHH |
|---|---|---|
| carried bodies > 0.5 m off the walls beneath | 0 | 0 |
| rows on the datum / footless at datum / elevated bodies alone | 0 / 0 / 0 | 0 / 0 / 0 |
| footed bodies refused as carriers (own feet off) | 8 (worst `Terminal4sBlue-STRT4__b1` 4.18 m) | 4 |
| body files | 1,369 | 1,622 |
| plan stage | 5.65 s | 31 s |

## 4. OPEN — waiting on the owner

1. **The LEMD read of 1.0.319** (supersedes 1.0.318). Sites from the last read
   (11ah): garage roof-top pavilions (`Terminal4_green-TEJ1`, were 15.8 m under the
   slab, now on it), the roof at 40.4841738 −3.5854522 (`green-TEJ3` panels, now at
   zero on `CNTRL`), roof planes at 40.4858658 −3.5676751 (`T4SAT_green-TEJ3`).
   Ask for COORDINATES, not shapeIDs — shapeIDs renumber every build.
2. **11ai-1 pad terracing at `building4`** (shape 83 in 1.0.317): one flat
   66,000 m² pad from unioned OSM footprints; car-park / service-road neighbours
   terrace 1–2.6 m against it (apron joints are 0.00 since 11af). Options: (a)
   extend 10l to parking/road frontages; (b) split unioned pads by terrain at the
   OSM footprint seams; (c) leave. Recommendation (b)+(a). Not dispatched.
3. **11af-1**: shapeIDs 935/936 the owner called tunnel ramps were two 3.5 × 12 m
   `building13` pad faces beside the T4 footbridge (40.4959, −3.5913). Confirm.
4. **Site 2 of 11ac** (40.4845169, −3.5828313): nothing within 199 m in the pack —
   probably a camera position; re-identify.
5. **§8 RETIRE THE SEAT** once the read accepts: delete `emit/rebake.py`'s seat,
   `emit/clusters.py` bodies, `airport/rigid.py`, `_one_file_one_delta`, the
   `.anchor_bak` vertex rewrite path, `o4_v2_rebake_result_*` JSON and
   `v2_rebake_replay.py seat|bodies|pairs`, `reanchor_dsf_objects.py`; the
   `[rebake] placement = "seat"` key goes with them; body formation moves under
   `airport/placement/`. Refuted mechanisms are DELETED, not gated.
6. **11g-1 / 11i** are answered (canopy stays with its columns and building) — no
   action pending.

## 5. OWED (recorded, not urgent; each has a RULINGS pointer)

* 8 LEMD + 1 OTHH footed bodies mis-anchored by the §9 low-side rule (11al/11am).
* Bodies standing only over a body of ANOTHER unit (34–38 at LEMD) — the carrier
  search is unit-scoped (11ag).
* Multi-anchor resources still skipped (72 LEMD / 61 OTHH) — §16 (1) names only
  the no-solid class (11aj).
* Plan-stage time: LEMD 5.65 s (bar 5), a vectorised surface sampler /
  `anchor_rule._inside` (920 k point-in-ring tests) (11al).
* `anchor_for`'s median-zero picking a roof vertex — contained by §13, not fixed
  at source (11w).
* Colonnade residue: feet sharing one triangle (mesh density under bodies, 11ad);
  whether the PATCH should reach bare-ground bodies beyond zones 1–2 (11ab).
* 50 orphan `__b*.obj` in the live LEMD pack from the pre-`body_files` 1.0.313
  write — inert, owner's call to hand-clean (11n).
* Duplicate `seat_feet_census.py` row in `tools/INDEX.md`; census blind spot on
  kept-whole records (11z).
* Harness: no LEMD `--base-arm` control exists at recent main shas — matched
  baselines need one 350 s build via the ledger.
* Earlier: SPJC two-tile warm (08h), 33.9 % groundside_ramp row, census cache key;
  Swift `SettingsLayout` still lists `auto_patch_engine` as a tile item.

## 6. How this session worked (protocol — keep it)

* Owner statements → RULINGS entry VERBATIM first, then attribution (scout,
  read-only), then the law (Fable spec §), then ONE lane (Opus, `subagent_type`
  lane/scout, `model: "opus"`), then merge (spawner owns the merge; conflicts keep
  both sides — spec/RULINGS conflicts are always additive), then the v2 twin set,
  then the app. Mechanism before fix: every fix lane got an interventional
  measurement first.
* Build economy: lanes replay the app's own plan on a PACK COPY
  (`tools/obj8_split_report.py --admit-skipped --write-pack COPY`, lane-local
  `O4_DSF_CACHE_DIR` / `O4_AIRPORT_MOD_CACHE_DIR`) — no airport build for
  object-stage work; ONE LEMD build for terrain work. The five-airport sweep never
  ran this session.
* App build: `/Users/noah/XPTerrainBuilder/scripts/make_engine.sh` then
  `DEVELOPER_DIR=/Applications/Xcode-beta.app /Users/noah/XPTerrainBuilder/scripts/make_app.sh`
  — the app MUST be quit (`pgrep -f XPTerrainBuilder.app` = 0); **`make_app.sh`
  bumps `Resources/VERSION` itself — never pre-bump**; commit `Ortho4XP/src/O4_Version.py`
  + `Sources/XPTerrainBuilder/Resources/VERSION` after.
* The owner's tiles: the app writes `<Patches>/+40-010/+40-004/` (`o4_v2_placement_LEMD.json`,
  `o4_v2_rebake_LEMD.json`, `LEMD.graded.json` — per-tile subdir) and rewrites the
  live pack's DSF (`Earth nav data/+40-010/+40-004.dsf` + `.anchor_bak` +
  `o4_placement_provenance.json`). These are rewritten by whichever build ran last —
  lanes COPY them to scratch before reading.
* Shell: the bash guard requires engine build/test commands with cwd =
  `Ortho4XP/`; the cwd flips between calls — use absolute paths and `git -C`.
* `SendMessage` was absent this session; waiters that poll `pgrep -f <pattern>`
  match THEMSELVES (two sat 13 h) — poll a PID or an artefact file.
* Engine log on app failures: `~/Library/Logs/XPTerrainBuilder/engine-stderr.log`
  (`[v2 placement] LEMD` lines).

## 7. Instruments added this session (all in `tools/INDEX.md`)

`obj8_split_report.py` (`--rows`, `--write-pack`, `--admit-skipped`; the §15/§16
census lines), `seat_feet_census.py --placement-plan --graded`, `dsf_placement_diff.py`,
`v2_rebake_replay.py order [--old-first]`, `python -m auto_patch_v2 explain ICAO
--sources`, `check_grade` reads `pad_relief`, `design.foot_row_diag`.

## Addendum — 2026-09-11 late (the 1.0.319 read round)

* **Main `1ef38ee7` = App 1.0.320** (engine 1.50.1766), suite 1,122 / 1 skipped. Four
  lanes merged: `v2owncut` (§16b), `v2basinring` (§14a), `v2relations` (§25/§26),
  `v2merge` (the conflict merge). RULINGS 2026-09-11an … 11at are the record.
* §1's census table and §4's open items are superseded: the bars 11aj–11am
  reported were BLIND to the eye (11ap). On the written 1.0.319 frame all six
  object sites read within 0.35 m; the old-terminal tunnel is gone; Terminal 2 is a
  132,884 m² pad joined to the apron (a multipolygon relation the loader had skipped).
* **OPEN for the owner**: the LEMD read of 1.0.320 (restart X-Plane first — objects
  are cached); 11ar-1 (six railway bores at LEMD: should a rail tunnel seed a
  structure?); 11ai-1 (pad terracing — the pad is now twice the size); then §8 seat
  retirement.
* **OWED (new)**: §24 (2) per-arc trench floor under the ring-cut wall; inner rings
  as holes (OTHH 773,141 m² dropped); plan stage LEMD 9.9 s / OTHH ≈ 50 s (the
  vectorised surface sampler); the rebake-replay vs written-plan disagreement
  (1,371 vs 1,417 files, anchor surface p90 5.84 m); §16b's census residue (the cut's
  atom is the triangle); `seat_feet_census --placement-plan` classing cut bodies
  "not in plan"; mint-green untextured pavement patches (draped `.pol`, unattributed).
* **Brief-template trap found**: `build_airport.py LEMD` without `--engine v2`
  measures the v1 engine — two byte-identical 12-min builds. Say `--engine v2` in
  every v2 lane brief.

## Addendum — 2026-09-12 (the 1.0.320 read round)

* **Main `e7098a28` = App 1.0.321** (engine 1.50.1767), twin set 1,169 / 1 skipped.
  Merged today: `v2atom` (§16c the connected component is the atom; rest-on carrier;
  rigid reach; replay tool under the write guard), `v2airsideedge` (§27 an airside
  edge makes a shape airside, owner 12c/12f), `v2meshgrid` (grid mesh sampler —
  mesh step 706 → 63 s), `v2ramp8` (tunnel ramps at 8 %, owner 12m). RULINGS
  2026-09-12a … 12p are the record.
* **OPEN**: the owner's read of 1.0.321 (restart X-Plane); T2 roofs (item 4 of 12b) —
  four of nine roofs still take a carrier metres off because the wall body is not
  in their stands-over candidate set (scout `v2t2roofs` attributing); 11ar-1 rail
  bores; 11ai-1 pad terracing (`building4` now 132,884 m²).
* **OWED (new today)**: OTHH plan stage 64 s (bar 45; 344,838 surface calls —
  attribute); the unsettled LEMD tunnel-ramp LP (`HARD SET NOT SETTLED`, max
  violation 1.30 m, pre-existing); a pre-existing red
  `test_tunnel_portal_fidelity::TestClearanceAnnulus::test_without_the_annulus…`;
  a one-run suite flake (2 failed, names not captured, 3 reruns green); the emitter's
  rounding headroom at an 8 % ramp; KCLT unbuilt under v2 (90 airside-edge flips);
  the two shared-repo files lane `v2atom` round 2 wrote (`Airport_mod_cache/Global
  Airports/+25+051.dsf.e0518fe0.text`, `o4_dsf_object_positions_+25+051.cache`) —
  owner deletes; `RIGID_CLUSTER_SPAN_MAX_M` 1,200 / `RIGID_REACH_COMPONENTS_MAX` 64
  are measured constants, not ruled keys.
* **Instrument law from today**: the TORN-SEAM census on the WRITTEN files
  (`obj8_split_report --torn-seams`) is the bar for rigid bodies — no per-body number
  can see a seam between two files of one solid. `v2_rebake_replay.py plan
  --sampler mesh` times the plan stage on the app's own sampler; the graded sampler
  is 60× off it. Every v2 lane brief says `--engine v2` (the bare entry runs v1).
