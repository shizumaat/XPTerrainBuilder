# Cycle 7 — The certified exit (the 82% round)

**Status: BINDING.** Evidence: the c6attr dossier (c6tip worktree,
tmp/c6attr_dossier.md; instrument proven surface-inert twice;
instrument diff preserved at tmp/c6attr/instrument.patch). The in-band
solver residual (24,854 rows, 82% of the battery) splits: convergence
~33-57% per airport, structure the rest — with measured owners. Mode:
BUILD-COMPLETE-THEN-DEBUG.

## Fix 1 (a) — the sweep budget prices the wrong process

`derive_sweep_budget` prices a diffusive relaxation with a LINEAR
(4 × diameter) bound — measured ~2 orders of magnitude short: pure
symmetric law CERTIFIES at HEAZ ×100 and reaches 0 rows ≥0.01 m at
HECA ×400; the production exit line "this exit is NOT budget
exhaustion" is FALSIFIED. Required: the projection exits on a
CONVERGENCE CRITERION — sweep in blocks, stop when the ≥materiality
over-cap count stops decreasing (or certifies), under an honest hard
safety cap; the exit line states which fired and the residual delta
of the last block. Same criterion for final_grade_projection (its
own certificate rises ENTRY 24,890 → EXIT 33,124 — the +8,234 is
expected to be the same underpricing; measure it after). Build-time
note: timing gates are suspended; the ledger tripwire (~2×) applies —
quote the wall-time delta honestly, and if a build goes ~2× flag it
for the final profiling round rather than self-optimizing.

## Fix 2 (c) — no zero-width DEM-datum box on an airside-coupled node

The fp#8 worst carrier is a 2-cycle: detached pad building172, band
WITHHELD, seat box (1.6576, 1.6576) — zero width at the groundside
datum — against an apron node AT its band floor 62.495, chord budget
0.0646 m over a 60.84 m step; each sweep moves both ±30.4 m and the
clamps restore them (drop boxes ⇒ worst 60.77 → 2.47). 343 detached
pads carry the class. The law: a detached pad's SEAT stays a seat
(movable-pads + §7 is production), but its box on an AIRSIDE-COUPLED
node may not be a zero-width DEM-datum pin — the box carries the
seat's LAWFUL width (the seat/coupler law's own band; the frontage/
band machinery already prices it) or the coupling edge is priced as
the declared step it lawfully is (split-level seats law: a sectioned
seat with a declared joint is the legal form for large relief).
Attribute per the seat machinery which form each pad takes
(decide-and-note); loud on any pad where neither form resolves.

## Fix 3 (b) — seed_rwy_seam depth (attribute-then-fix)

All 610 strictly-pinned seam anchors are conservative-certificate
witnesses; type specimen: 51.19 m |dz| between pinned anchors over a
33.38 m cap-graph budget (shortfall 17.81 m). Depth not breadth
(freeing all seeds moves −1.3%). Attribute FIRST (the anchor-law
precedent — cycle 4/5 twice found pins carrying ride): are the seam
pin VALUES world-dependent (ride in a pin — fix at the pin's
authority), or is a lawful relief form missing between two true pins
(complete the law)? Then fix at the attributed site.

## Fix 4 — the interval/slab layer (attribution REQUIRED first)

Largest owner of the structural floor (production-minus-boxes at
49,600 sweeps: 14,152 vs pure-symmetric 2,894 — the signed
INTERVAL/slab layer owns the gap), verdict (b)/(c) UNATTRIBUTED. The
lane's FIRST instrumented measurement: decompose the slab-layer
residual by slab class (rod/§10.1/zone/witness) via the replay, then
fix only what attributes cleanly; STOP-and-report the rest.

## Fix 5 (d) — small: the certificate names flat-group edges

1,184 <UNATTRIBUTED> edges (flat-group representatives) carry the
solve's worst residual; family_by_pair must resolve through the
group→member map.

## Chores (same lane, cheap)

- Q4 closure: the P∘D +219 interaction — worktree at 36e0926 + one
  HECA dem1 build + census (~9 min), label the interaction's owner.
- TOOL: extend tools/interval_reach_replay.py (near-fit, DO NOT fork)
  with --sweeps/--arm and the missing dump keys
  (node_bounds/env_band/gs_pin_nodes/witness_*) so it replays fp#8
  faithfully; INDEX/README row in the same commit.

## Acceptance

- HECA dem1 + HEAZ dem1 (and canyon spot-check): SOLVE EXIT
  certificate before/after per fix (the per-fix ladder — fix 1 alone,
  then +2, then +3/4); target: CERTIFIED exits or the quantified
  honest residual per remaining class. fp#8 worst off the 60.77
  class.
- Census per airport vs the frame of record (checkpoint block);
  battery direction quoted honestly.
- Suite + twins green; new twins: convergence-criterion exit,
  zero-width-box refusal, flat-group attribution.
- Wall-time delta quoted (tripwire honesty).

Budget: ~6-8 builds + replays. Materiality 0.01 m; attempt cap 2 per
fix; heartbeat; foreground; no real-DEM; no shared-repo writes.
