# Cycle 9 — The road feed joins the graph (the real D′ finisher)

**Status: BINDING.** From the c8fin STOP dossier 2: the one graph
carries only apt.dat row-1206 service routes (HECA 5 / KCLT 15 / SPJC
15 / HEAZ 0) while the ROAD FEED (HECA 705 lines / 97.9 km, KCLT 320,
SPJC 84 after free-road scoping) carves the slice and never becomes a
centerline — so D′ stands (~3,900 gs rows ≥10 m) and the +244 honest
service_road/service_junction shortfall has no band to seat it. Owner
law: ONE graph; mouths seat like apron-edge frontage; receiver-only;
the projection PARTITIONS (ratified); no terrace joints across any
road. Mode: BUILD-COMPLETE-THEN-DEBUG. CUT LINE 03:30 PDT — land what
is merge-ready, STOP-and-report the rest; a partial that passes its
gates beats a complete miss.

## The mechanism (named by the dossier)

Register `layout._slice_service_subsegments` (the road-feed roads as
sliced) as service route edges in `build_context` — WITH the sidecar
export and the census reader in the SAME landing (build_context is
shared with the validator: lockstep, the coupling-and-census rule).
Groundside reach only (the airside exclusion stands); their mouths
seat via the landed apron-edge-band path; their 8% budgets via the
existing service pricing. Q4 GATE guards: per-cell airside ≤ the
c8fin state (HEAZ 91/162 · SPJC 357/613 · KCLT 473/887 · HECA
4,129/4,006) — a groundside round may not raise airside.

## Acceptance (vs the c8fin matrix, both worlds)

- D′ collapses: ≥10 m gs (~3,898) → ≈0 minus disconnected exemptions;
  way −12518's 132 at-seed nodes seated; on-DEM emitted → exemptions
  only; the +244 service shortfall seats (service_road/junction
  within_shape rows fall, not just move).
- Q4 gate 8/8 vs the c8fin airside state. Battery both worlds with
  --magnitude-bands; suite FAILED-diff vs the 11 standing; twins
  (feed-edges in graph + sidecar + census agreement; mouth counts
  quoted). Materiality 0.01 m; attempt cap 2; heartbeat; foreground;
  no real-DEM; no shared-repo writes.
