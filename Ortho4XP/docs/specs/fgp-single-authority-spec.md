# final_grade_projection: one authority, one law
# (Fable spec, 2026-09-01. The airside-zero blocker, named by four
# independent lanes: RULINGS 2026-09-01n/p/q/r/s. Acceptance instrument
# exists: the airside-scoped certificate, 01-air7.)

## The defect, as measured (do not re-derive; cite)

FGP rebuilds its own constraint graph after the solve and then acts as
a SECOND AUTHOR over a surface the solve had already made lawful:

1. **It re-authors lawful values.** 71–94 % of every apron class has an
   endpoint FGP moved ≥0.1 m off the solve's value (p50 2.0–3.6 m);
   at specimens the solve leaves an apron flat and lawful at 102.2–102.7
   and FGP emits 97.4–101.35 (01p). Solve-exit values are byte-identical
   across arms; FGP writes the difference (01s).
2. **Its rebuilt graph disagrees with solve law.** The solve-minted
   lattice/station/no_step law is absent from FGP's constraint set
   (`membrane_conform`'s own comment); FGP mints its OWN hard
   contradictions at entry — 1,602 over-cap both-hard pairs from
   `gs_weld`/`service_ring` sources against the solve exit's 759, all
   groundside-service (01r).
3. **Its band clamp is a second author.** air5's worst mover (+8.05 m)
   IS the clamp lifting a POCS-dragged node to a *contradicted* held
   profile value (01r §2).
4. **It never reaches its own emitted carriers.** `apron_lattice_emit` /
   `apron_spine_station_emit` are minted at the SOLVE writeback and never
   refreshed, so pass-2 movement never reaches them; making them truthful
   exploded airside 1,075 → 2,120, proving the stale emit MASKS interior
   drift to 15.7 m (01q).
5. **It never certifies.** 20–21 UNCERTIFIED exits per build; on the
   airside scope the residue is projection residual + mixed rows, NOT pin
   infeasibility (airside both-hard: HECA 3, CYXY 1, SPJC 3 — 01r/air7).

Exonerated, do not re-investigate: airside feasibility (one 3 mm
contradiction), the emit consensus (zero groundside wins), the apron
relief prior, the hard-set contradiction premise.

## The law this restores

ONE SOLVE, ONE AUTHORITY (the campaign's own architecture): after the
solve, no pass may author a value the solve owns except under a law the
solve itself stated. FGP's legitimate job is PROJECTION — moving values
that violate law INTO law — never re-deriving what the law is.

## The change, in order (each step measurable alone)

**S1 — FGP consumes the solve's law.** FGP's constraint set is the
solve's published law (the same registers the solve certified against),
not a rebuilt graph: the lattice/station/no_step families join it; the
`gs_weld`/`service_ring` hard sources are re-derived only where the
solve did not already state a value, and never in contradiction with a
solve-stated one. Acceptance: FGP entry both-hard over-cap → ≤ the
solve exit's own count; no new families at entry.

**S2 — the band clamp yields to the solve.** Where the clamp would move
a node the solve stated lawfully, it does not; where it must move one
(a genuine band violation), it reports the move with its authority
(the named-writer discipline every other pass now follows). Acceptance:
zero unauthored moves in `who_wrote`'s displacement census; air5's 827
class → 0.

**S3 — the carriers tell the truth.** `apron_lattice_emit` /
`apron_spine_station_emit` refresh after pass 2 — BUT ONLY AFTER S1+S2
land, because 01q proved the stale emit is masking interior drift; the
1,075 → 2,120 explosion is the measure of what S1/S2 must fix first.
Acceptance: refresh is byte-neutral on a tree where S1/S2 hold.

**S4 — certify.** The airside-scoped certificate (air7) reads CERTIFIED
at HECA/CYXY/SPJC, or its residual list is the honest remaining work.

## Discipline

30l consumer census before S1 (FGP has many readers). One step per
round, each gated and default-OFF until measured, each with its own
closing arm and the certificate quoted. Senior invariants hold
throughout: runway profile 0 (H7's gate), no senior movement, basin and
tunnel invariants byte-held. A step that cannot land without breaking a
senior invariant STOPS and reports — never trade one law for another.

## Scheduling

This is the FIRST POST-BETA ROUND unless S1 lands green before the
Wednesday 20:00 freeze. S1 may be attempted pre-freeze gated OFF; it
merges pre-beta ONLY if it is byte-neutral with the gate off AND green
with it on. Everything else waits for the owner's Thursday read.
