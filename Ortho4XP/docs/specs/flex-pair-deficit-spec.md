# Runway flex closes the anchor-pair deficit — spec

Author: lead (Fable), 2026-08-08. Charter: the second-budget-channel
attribution (fabric-phase-b-spec.md, LEAD RULINGS 3 banner + the
attribution artifacts in .claude/worktrees/fabricB/Ortho4XP/tmp/
fabricB/attrib_off.*). Verdict adopted: there IS no second budget
channel (that premise was a CROSS-TREE arm misidentification — the
same instrument-error class as ruling-1's, owned); the budget is
law-true (cap × lawful route, 6.4 m slack when the flex works); the
DEMAND is (a)-lawful (54.400 m raw CIFP separation between parallel
runways — HECA is not flat; DEM contributes 4 mm); the SHORTFALL is
MINTED by the flex retirement rule — the standing
flex-self-anchor-lock class, fourth appearance: solve.py:588-599
retires a bin after two zero-grant rounds on the premise that
refusal is a law verdict; measured false (the same bins retire in
both arms; one arm still reaches 102.210, the other stalls at
110.190 — the refusal is a property of the constraint population
present that round, not of the airport).

## The fix (either shape acceptable; prefer 2 if equal)

1. Retirement conditional on RESIDUAL DEMAND, not apply-failure
   count: a bin stays armed while the envelope deficit it was raised
   for is unsatisfied; if a contradictory anchor pair stands at flex
   exit, retired bins on the route between those anchors re-arm for
   ONE final round with the PAIR'S deficit as the explicit target.
2. The anchor-pair deficit becomes a FIRST-CLASS flex demand — the
   flex closes the pair directly instead of discovering it through
   per-station envelope demands refusable one at a time.

Untouched by construction: the reach band, R-a route-transparency,
the ruling-1 cross-section binding.

## Acceptance (the two captured arms are the fixture)

1. `xsec_heca` (vertex-hits ON): the 1.741 m shortfall becomes
   slack; zero contradictory pairs; rc 0.
2. `xsec_heca2` (vertex-hits OFF): numbers UNMOVED (byte-comparable
   patch or attributed sub-materiality).
3. `O4_XSECTION_VERTEX_HITS` re-arms default-ON (its parking reason
   is dissolved) — CYXY gate numbers reconfirmed (transverse ≈28,
   the wide-corridor lean class CLOSED).
4. Battery matched controls; twins for the retirement rule (a
   zero-grant round with standing residual demand does NOT retire;
   the final-round re-arm fires exactly once; the pair-deficit
   demand is register-visible).

## Sequencing and budget

AFTER the fabric battery merges and the owner's app ships (no code
on lane/fabricB mid-battery). Budget: 4 builds + reserve 2, cap 6.
Deviations: STOP-and-report for Fable review.
