# Road pair law is receiver-pass law — spec (road-feed gate path)

Author: lead (Fable), 2026-08-07. Charter: this is ENFORCEMENT of
standing law, not new law — ONE-graph ruling binding point 2 (the
band flows airside → groundside; groundside is receiver-only, ZERO
pull back) + airside-is-king. Cycle-10's M1 measured road-role pair
law binding the AIRSIDE pass as the largest single carrier of the
road feed's airside regression (−262 of +444 at 10k when withheld;
+22 at −500). A groundside road's law constraining the airside solve
IS a pull back, and is therefore already forbidden.

## The work (in lane/c9feed, tip b3cfa55)

1. **Receiver-only becomes the default**: the M1 knife's construction
   (`one_solve._withhold_road_pair_law` — road-role pair entries move
   to the receiver pass, never deleted) is the production form. Keep
   a default-OFF probe gate for the OLD form
   (`O4_PROBE_ROAD_PAIR_LAW_AIRSIDE=1`) with the standard inertness
   twin, so the comparison stays one env var away.
2. **Attribute the residual +182** (10k own-frame airside 4,198 vs
   base 4,006 with receiver-only on): class decomposition FIRST
   (which families, which sites, offline on the knife-arm patches);
   then at most TWO pre-registered per-candidate knives using the
   committed probe gates if decomposition alone cannot name the
   carrier. Target: ≥90% of the 182 rows attributed to named classes.
3. **Re-read the road-feed gate** and produce the owner's merge
   table: HECA 10k / −500 airside (own + base frames), battery
   matched controls, groundside delta (the honest +244 class),
   remaining named residuals. The MERGE of lane/c9feed stays an
   OWNER decision — this spec ends at the table.

## Acceptance

1. Receiver-only default reproduces the M1 knife-arm bodies exactly
   (expected `2a2e26f13423` @10k / `f7de0c4855a2` @−500 if only the
   default flips; a body change beyond the flip is a STOP).
2. −500 airside ≤ 4,027; 10k airside ≤ 4,198 (no regression vs the
   knife arm).
3. Zero groundside LAW changes; groundside numbers move only through
   the receiver pass.
4. Twins: gate inversion (new probe gate restores the old form;
   default is receiver-only), plus the existing probe-gate registry
   updated.
5. The +182 attribution table with per-class row counts and sites.

## Budget

2–4 HECA-class builds (cap 6), decomposition offline first. No
timing claims. Deviations: STOP-and-report for Fable review.
