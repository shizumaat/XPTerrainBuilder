# Scorer v2 — class-change boundary cuts (owner-approved 2026-08-29d)

Founding site: HECA's authored apron BACK EDGE, 30.1135641,31.4088047 →
30.1149573,31.4106698 (owner: a wall; apron elevated above groundside
parking lots; the package sends no pavement across the line). Attributed
by lane/apronedge (merged instrument, who_wrote --footprint): apron
shape 584 is BORN spanning the line at pipeline.py:3833's global-slice
face (401,100 m² at first write; no adoption — §H2 refuted at this
site). The pipeline unions every paint layer (apt.dat rings + DSF
object ground paint) into one pav_union with no cut at authored
boundaries; the scorer roles the whole face as apron. The package's own
layers ALL change at the line: apt#111 ring 0.91 m from it,
asphalt_bright.obj 0.01 m; behind it asphalt.obj + apt#57 (groundside).

Standing context folded in: heca-round4 Amendment 1 ("the scorer's
verdict on the severed piece is the missing lever"; §H3 severance
retired-kept-gated, IoU 0.8221); the round-5 item-3 scorer docket;
memory pavement-classification-overhaul (scorer v2 owns roles; owner
rulings become hard gates; join owner shapeIDs before editing the
slice).

## Law

1. CLASS-CHANGE BOUNDARY CUT: where the union face spans an AUTHORED
   layer boundary whose two sides carry DIFFERENT EVIDENCE CLASSES
   (airside-class evidence — apt.dat airside rows, airside paint — on
   one side; groundside-class evidence — lots, groundside paint,
   apt#57-class rows — on the other), the slice CUTS the face at the
   authored edge and each side is scored separately, with its own
   side's evidence only. The discriminator is the CLASS DISAGREEMENT,
   never a page boundary, never a bare gap (both measured too broad:
   187 rings span pages; the source union is one component).
2. THE MEASURED TOO-BROAD PREDICATES ARE REFUSED as cut triggers
   (component gap, page boundary, bare row-110 depth) — the lane's
   population numbers are the refutation record.
3. Severed groundside pieces take groundside law (lots stay lots; the
   free-road/groundside terrace laws apply); the authored edge with an
   elevation step is the declared wall/terrace class. Airside pieces
   keep airside law. No piece is deleted — re-roled, never removed.
4. Owner rulings remain hard gates through the re-roling (the standing
   scorer rule); shapeID joins for owner-referenced shapes preserved.

## Acceptance (SITE-FIRST)

- The founding line: osm_site --line over the owner's two coordinates
  shows NO airside pavement crossing; the wall/step stands at the edge;
  the lots behind are groundside-roled at groundside law.
- Owner item-4 site (30.114984,31.4107959 — this apron's corner):
  report what changes; the road now solves against the corrected apron
  edge.
- Five airports (HECA/CYXY/SPJC/OTHH/LEMD) censused control vs arm:
  law-true totals not worsened OR every delta attributed to re-roling
  (a piece moving from apron law to groundside law re-prices — split
  re-roled-population deltas from same-role regressions; zero same-role
  regressions is the bar).
- The §H3 severance gate and its IoU evidence reconciled: state what
  supersedes what.
- Below-bar = STOP with residuals quoted. Twins: the cut discriminator
  both ways (class change cuts; same-class boundary never cuts), the
  refused predicates, hard-gate preservation, founding-line acceptance.
