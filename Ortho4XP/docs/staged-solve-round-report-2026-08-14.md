# Staged-solve round report (closed to in-sim, 2026-08-14)

Spec: docs/specs/staged-solve-round-spec.md. Every lane merged; the
agreed architecture is fully implemented (owner no-deferral ruling
honored); hard-law build-time check: PASS (residual owner-approved,
publish/pair perf round chartered post-in-sim).

## The architecture, landed

1. GEOMETRY FREEZE + STAGE PARTITION: all solve-consumed geometry
   completes before solving; stage A (airside) is structurally free of
   groundside variables (21/21 couplings railed; a stage-B write of a
   stage-A row is a twin failure); one node list + one graph + one
   band published at the freeze.
2. THE SINGLE PROJECTION: the mid/late pair collapsed to ONE call
   (AST-twinned). The late call was proven a DEGRADATION SHIELD by
   building-and-refuting its defense twice independently; refinement
   was proven ALREADY value-preserving (re-projection class 19 rows
   at HECA, decomposed; band-seal authorship stands-attributed).
3. OBJECT PADS, EMISSION-TIME RELATIVE: read-back retired, blend
   annuli retired, cap reads the plate's landing ground (towers
   refused by construction). DETERMINISM PROVED: sequential tile
   builds byte-identical at patch, DSF and mesh level — the
   689→723→736 ratchet is dead, mechanism identified and killed.
   Intent metric: OTHH 86.5% of structures need NOTHING (median
   0.053 m off ground); HECA 0.1% (median 11.5 m — shared-datum
   authoring bounds pack-pristine, y-bake lawfully serves the rest).
4. WELD-OR-GAP: stacked-conflict walls, groundside terrace walls,
   retreat feathers RETIRED (compile-time-inert gate; deletion sweep
   after the owner's in-sim test). Step families FELL at retirement —
   the walls were MINTING the steps beside them. Seawalls exempt by
   module boundary; VHHH coast byte-stable (47 walls / 54.7 km).
5. RIM POCKETS DEFAULT ON (spines unconditionally stage B): the HECA
   knoll grades (93.7-class); off-face channel closed by the stage
   ruling; near-cap churn is the ruled lawful band.
6. CENSUS UN-BLINDED: 11,932 unread rows restored battery-wide;
   service-road transverse read at the generator's constant; runway
   crown reader landed; drainage retired NARROW (taxiway/road surface
   curvature only — spines, adjacent-ground slopes, aprons' FAA half,
   crown all stay); DEM deviation not reported at all (owner law).
7. INSTRUMENTS: solve-capture v2 (DEM boundary leak closed — OTHH
   replay quarantine LIFTED, byte-for-byte); seam audit
   (O4_GEOM_SEAM_AUDIT); input-fingerprint census; guard-armed
   profilers; solve_cut cwd refusal; tile cfg derives from global
   defaults; imagery deletion guarded by error class (the KCLT
   orthophoto-loss incident is structurally impossible).

## Final consolidated bodies (all guard-clean)

HECA 3053349c0b26 (3286) · KCLT bbd2ed66a8a4 (2634) · OTHH
aaf3ce394d01 (2237) · CYXY 2c3331baccb1 (428) · KSTJ ed91563060d8
(175). KSTJ census-identical to its reference; KCLT airside delta
fully enumerated (3 NEW ≤0.27 m in ruled classes); HECA/CYXY/OTHH
lane-attributed row-by-row.

## Build time (exclusive 2-run medians vs the P4 baselines)

IMPROVED: SPJC −19.8, KCLT −17.8, CYXY −3.7, SPLP −0.8.
APPROVED RESIDUAL (owner 2026-08-14): OTHH +46, HECA +33, HEAZ +6 —
the architecture's measured floor (one-graph publish ~+18 net, pair
law +14 on fewer executions, rim pockets +10, flat-site +13, single
projection −15). All optimization suspects exonerated (≤1.5 s,
solvereg review); recovery paths are OUTPUT-MOVING (publish collapse;
the GradeShape flag-flavor law gap) — chartered as the post-in-sim
perf round with census adjudication. Inter-block thermal spread
~5-9% observed and noted.

## Owner in-sim list (acceptance)

HECA: the knoll (30.11367,31.40864 — grades for the first time), the
western apron (towers refused), the corridor spines (hump halved,
zero cap-riding at the crest), general apron/junction look under
weld-or-gap. VHHH: the coast (walls must stand vertical). KCLT: ramp
corridor, strip area (the pre-existing 7.2 m strip↔strip site is
docketed, unchanged). OTHH: the flats (86.5% untouched objects),
corridor areas. Determinism: rebuild any tile twice — identical.

## Post-in-sim queue (owner-sequenced)

1. Dead-code deletion sweep (gate verified compile-time inert).
2. Publish/pair perf round + flag-flavor ruling (output-moving, census
   -adjudicated).
3. Ratification triage (2026-08-12a list) against the final design.
4. Stage-B groundside docket (weld-conform band, KCLT strip site,
   band-seal clamp findings).
