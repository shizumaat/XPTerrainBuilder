# KAFW + KDFW new-fixture triage (wave-3 step 6)

**Attribution only — no fixes, no timing, no shared-repo writes.**
Lane `lane/fixtriage` off `main` 47e83ed (includes the F3c graded-handoff
merge). Every number below comes from `tools/harness/build_airport.py`,
`tools/harness/census.py`, `tools/census_rows_diff.py`, `tools/osm_site.py`
and production's own library functions. Nothing here re-counts a defect.

---

## 0. The frame, and the battery reproduction

| airport | build | shapes | law-true | adjudicated | airside-for-acceptance |
|---|---|---|---|---|---|
| KAFW | `fixtriage_kafw` body `551fd7c69d4f`, rc=0 | 732 | 746 | **341** | **65** |
| KDFW | `fixtriage_kdfw` body `dca97081eb65`, rc=0 | 3497 | 2538 | **569** | **284** |

The 2026-08-16 battery recorded KAFW 365/89a and KDFW 576/291a. The deltas
are **−24/−24** and **−7/−7**, and they are fully attributed:

1. The battery patches (artifact-ledger blobs `KAFW_20260816T083441`,
   `KDFW_20260816T085141`, tree `f575f9bd`) re-censused under **this** tree's
   law give **exactly 365/89a and 576/291a**. So F3c's `check_grade` half is a
   census **no-op** at both fixtures — the battery frame is reproduced to the row.
2. `census_rows_diff.py` battery-vs-lane, same law knobs, same axis frame:

   | | EXACT | MOVED | GONE | NEW |
   |---|---|---|---|---|
   | KAFW | 746 | 0 | 24 | 0 |
   | KDFW | 2538 | 0 | 7 | 0 |

   Every GONE row is `drainage_spine::runway|runway [airside]` — KAFW ways
   `-10753` @(32.989921,−97.320821) and `-10751` @(32.998811,−97.322697);
   KDFW way `-13544` @(32.906895,−97.049739).

**Verdict: the builds reproduce the battery. The entire delta is F3c's
emitter-side graded handoff (`gap_fill._spine_interval` →
`grade_law.drainage_spine_interval`), exactly as the brief anticipated.
No STOP.** The only code between the two trees is F3c
(`gap_fill.py`, `grade_law.py`, `check_grade.py`); nothing else moved a byte.

---

## 1. KAFW — 341 adjudicated / 65 airside

FAA ruleset (declared and active, from the sidecar). 106 law-true sites,
90 carrying an adjudicated row, **49 ACTIONABLE**, amplification 7.0 rows/site.

### 1.1 Family × role-pair × side

| family | role pair | side | rows | worst \|de\| | worst site |
|---|---|---|---:|---:|---|
| drainage_minimum | apron\|apron | airside | 405 | 1.140 | *VERSION-DEFERRED, not adjudicated* |
| transverse | service_junction\|service_junction | gs | 112 | 1.739 | (32.97999,−97.31148) `-10060` |
| within_shape | groundside_pavement\|groundside_pavement | gs | 72 | 2.940 | (32.98230,−97.32678) `-10619` |
| transverse | service_road\|service_road | gs | 49 | 1.048 | (32.97869,−97.31881) `-10531` |
| runway_crown | runway\|runway | **airside** | 34 | 0.200 | (32.98736,−97.31782) `-10365` |
| within_shape | service_junction\|service_junction | gs | 27 | 1.450 | (32.99255,−97.31042) `-10156` |
| strip_arc | graded_strip\|graded_strip | **airside** | 24 | 2.080 | (32.97843,−97.31895) `-10633` |
| within_shape | service_road\|service_road | gs | 12 | 2.800 | (32.99881,−97.31820) `-10520` |
| mid_edge_step | apron\|building | **airside** | 3 | 0.650 | (32.97707,−97.31935) |
| mid_edge_step | service_junction\|service_road | gs | 2 | 0.738 | (32.97894,−97.32697) |
| transverse | junction\|junction | **airside** | 2 | 0.436 | (32.97736,−97.31390) |
| vertex_to_edge_step | apron\|building | **airside** | 1 | 0.750 | (32.97709,−97.32054) |
| plane_gradient | service_junction\|service_junction | gs | 1 | 0.420 | — |
| strip_longitudinal | graded_strip\|graded_strip | **airside** | 1 | 0.400 | — |
| lateral_contiguity | service_junction\|service_junction | gs | 1 | 0.070 | — |

Airside 65 = runway_crown 34 + strip_arc 24 + mid_edge_step 3 + transverse 2 +
vertex_to_edge_step 1 + strip_longitudinal 1.
Top-90 % of rows: transverse (161), within_shape (111), runway_crown (34) — 306
of 341; adding strip_arc reaches 97 %.

**Every within_shape and transverse row is SAME-WAY** (an interior chord of one
shape; 0 cross-way rows). Grade buckets:

| class | 0–2 % | 2–5 % | 5–8 % | 8–15 % | ≥15 % |
|---|---:|---:|---:|---:|---:|
| transverse service_junction (112) | — | 72 | 36 | 4 | — |
| transverse service_road (49) | — | 32 | 8 | 9 | — |
| within_shape groundside_pavement (72) | — | — | — | 49 | 23 |
| within_shape service_junction (27) | 2 | 14 | — | 4 | 7 |
| within_shape service_road (12) | — | — | — | 6 | 6 |

### 1.2 Classification

**KNOWN — road-face chord exposure awaiting the chord limiter** (wave-3 step 1,
`docs/specs/road-chord-limiter-spec.md`): verified at the source —
`groundside._grade_limit_groundside_chords` walks `ROLE_GROUNDSIDE_PAVEMENT`
**only** (groundside.py:3759). The road family has no chord limiter.
**But the limiter as specced caps at the ROAD LIMIT (8 %), and 148 of the 161
road transverse rows sit at 2–8 %** — under the road chord cap and over the
`SERVICE_ROAD_MAX_TRANSVERSE` = 2 % cross-section cap
(`config.transverse_cap_for_longitudinal_cap`). *The chord limiter can pay at
most 13 transverse + 37 within_shape road rows at KAFW; the other 148 are a
different law (the road's 2 % transverse), and the dossier flags that so the
limiter lane does not book them.*

**KNOWN (corollary, lot side) — the lot inherits its road welds.** The worst
groundside site, lot way `-10619` @(32.98230,−97.32678), 12 rows, worst 2.940 m
at 14–21 %: the ring's low nodes `-4384`/`-4383` (206.44/206.45) are shared with
`service_junction -10317` (205.43–208.13) and its high node `-1665` (209.33) is
shared with `service_junction -10109` (208.16–209.38) — two road faces 13.6 m
apart disagreeing by 2.89 m across the lot. The lot's own limiter cannot fix
this: at emit the road wins the shared node
(`layout.AUTHORITY_PRECEDENCE`, airside-first), and the roads themselves are
unlimited. Clause 3 of the chord-limiter spec (corridor coherence over one
shared-node key space) is written for exactly this.

**KNOWN — adjacent-ground band**: `strip_arc` 24 + `strip_longitudinal` 1
(airside, all `graded_strip`). The canon already attributes the
strip_arc/strip_longitudinal family to the adjacent-ground band
(STATUS 20260815g, string-bend retirement note). No new mechanism claimed.

**KNOWN — apron↔building step marginalia**: `mid_edge_step` 3 +
`vertex_to_edge_step` 1 on `apron|building`, worst 0.750 m, all at
(32.97707,−97.31935)/(32.97709,−97.32054) — the frontage/pad-seat step class.

**NEW (mechanism unattributed) — the crown realisation residual**: see §1.4.

**NEW (mechanism unattributed) — the road family's 2 % transverse has no
enforcer that holds**: see §3.

### 1.3 The build's own certificate — where the groundside bulk is authored

Not inferred; printed by the build:

```
final#1 ENTRY: over_cap=1023 law edge(s) (195 both-hard) in 16 violating famil(ies)
   298 unified:service_junction   152 junction:-   108 unified:junction
    99 unified:service_road        56 apron:-       52 service_junction:-
final#1 EXIT : over_cap=326 law edge(s) (195 both-hard) in 11 violating famil(ies)
   143 unified:service_junction    72 unified:service_road   29 service_road:-
    23 service_junction:-          16 service_road:road      12 unified:service_junction:spine
[empty-interval] phase-A harmonic split an EMPTY polytope at 121 node(s) (102 by >0.01 m)
[writeback-band] 2 solved value(s) CLAMPED into the unified reach band (worst −0.14 m)
[seam-pins] 40 seam pin(s) at DEM; 4 pin-pair(s) over the 1.5 % cap the solver must
            step through — worst 4.78 m excess (14.65 % over 36.4 m)
```

So KAFW's 272 groundside rows are **a solver-exit residual on the road family**:
326 law edges left over cap at the final projection, **195 of them BOTH-HARD**
(both endpoints pinned — a hard-anchor contradiction the projection cannot move,
not a convergence question), plus the SM3-flavoured 121 empty-polytope
midpoints. The missing chord limiter is the *second* half of the story, not the
first: it would clamp what the solve leaves, and the spec says so.

The `[seam-pins]` line is worth the owner's eye on its own: **4 tile-seam DEM
pin-pairs are mutually infeasible at KAFW** (worst 14.65 % over 36.4 m against a
1.5 % cap). Hard pins at DEM that contradict each other are the same shape of
defect as the KDFW EAT pins in §2.4.

### 1.4 KAFW runway-seeding residual — the verdict asked for

| family | rows |
|---|---:|
| `within_shape::runway` | **0** |
| `runway_end_skirt` | **0** |
| `raoa` | **0** (ICAO-only family; KAFW is FAA) |

**Neither the R8 diagonal/join class nor the phantom-crown-offset class mints a
single row at KAFW.** Measured, not assumed — `crown_drops` coverage of the
runway ring (`check_grade._crown_drops_by_nid`, the census's own reader):

| | KAFW | HECA (lane/resid, for contrast) |
|---|---|---|
| runway-family ways | 2 (`runway`, no `runway_crossing`) | — |
| ring nodes | 260 | 521 |
| carrying a declared drop | **254** | 427 |
| undeclared | **6** | 94 |
| adjacent ring pairs with exactly one endpoint declared (PHANTOM offset) | **4** | 267 of 1,149 |
| `crown_centerline` entries | 0 | 0 |

KAFW's crown field is essentially complete, so `crown_pair_offset` has almost
no phantom endpoint to default to 0 — the HECA class does not transfer.

What KAFW's runway family *does* carry is **`runway_crown` 34 rows, airside,
2 sites (one per ring, `-10365` 27 rows / `-10366` 7 rows)**, worst realised
fall 0.200 m against a declared drop of 0.230 m, worst grade shortfall
0.088 pp / 0.095 pp, accumulation 0.243 m / 0.068 m. Both sites are ACTIONABLE
only by the runway-family always-actionable rule.

Reading the emitted ring against the `crown_spine` breakline for all 256
declared ring nodes (production's own `_nearest_ridge`): realised − declared is
**median +0.013 m, sd 0.034 m, range −0.052 … +0.181 m** — a symmetric wobble
about the declaration, not a taper. Correlation with distance from the ring
node's ridge foot to the nearest spine vertex is **−0.035**, so it is *not* a
ridge-interpolation artifact of the reader.

### 1.5 Arm 1 (spent) — emit decimation is REFUTED

Hypothesis: the ±0.05 m crown residual is emit decimation's z-tolerance
(`emit_decimate.Z_TOL_AIRSIDE_M` 0.02 m, `Z_TOL_BOUNDARY_M` 0.10 m).
Arm: `O4_EMIT_DECIMATE=0 build_airport.py KAFW --tag kafw_nodecim`, rc=0.

| family / roles / side | base | no-decimate | Δ |
|---|---:|---:|---:|
| runway_crown runway airside | 34 | 34 | **0** |
| strip_arc graded_strip airside | 24 | 24 | **0** |
| transverse service_junction gs | 112 | 121 | +9 |
| within_shape groundside_pavement gs | 72 | 74 | +2 |
| within_shape service_junction gs | 27 | 28 | +1 |
| transverse junction airside | 2 | 1 | −1 |
| drainage_minimum (deferred) | 405 | 390 | −15 |
| **totals** | **341 / 65a** | **353 / 64a** | +12 / −1 |

**Decimation authors none of it.** The crown and strip families are row-stable;
the road/lot families get slightly *worse* without it (decimation was mildly
removing over-cap vertices). The hypothesis is dead; the residual's author is
upstream of emit. Recorded as a refutation, not a result.

---

## 2. KDFW — 569 adjudicated / 284 airside

FAA ruleset. 193 law-true sites, 111 carrying an adjudicated row,
**63 ACTIONABLE**, amplification 13.2 rows/site (max 231).
`triangle_plane_unresolved = 30`, `seam_pins = 4`.

### 2.1 Family × role-pair × side

| family | role pair | side | rows | worst \|de\| | worst site |
|---|---|---|---:|---:|---|
| drainage_minimum | apron\|apron | airside | 1969 | 1.290 | *VERSION-DEFERRED* |
| transverse | service_road\|service_road | gs | 178 | 1.497 | (32.91218,−97.04096) `-10439` |
| within_shape | apron\|apron | **airside** | 119 | 1.050 | (32.90567,−97.04587) `-11474` |
| within_shape | junction\|junction | **airside** | 113 | **21.530** | (32.87912,−97.05164) `-10253` |
| transverse | service_junction\|service_junction | gs | 45 | 1.346 | (32.90027,−97.04169) `-12950` |
| transverse | junction\|junction | **airside** | 31 | **21.509** | (32.87906,−97.05255) `-10254` |
| within_shape | service_junction\|service_junction | gs | 29 | 1.380 | (32.89979,−97.04167) `-12950` |
| lateral_contiguity | service_junction\|service_junction | gs | 14 | 0.070 | (32.88574,−97.03434) |
| mid_edge_step | service_junction\|service_road | gs | 13 | 1.509 | (32.88289,−97.03985) |
| strip_arc | graded_strip\|graded_strip | **airside** | 6 | 0.460 | (32.90013,−97.05004) |
| drainage_spine | apron\|apron | **airside** | 4 | 0.875 | (32.89296,−97.03373) `-13580` |
| transverse | apron\|apron | **airside** | 4 | 0.241 | (32.89942,−97.03519) |
| strip_seam_tear | graded_strip\|graded_strip | **airside** | 2 | **21.740** | (32.87897,−97.05138) |
| mid_edge_step | junction\|runway | **airside** | 2 | 3.638 | (32.87906,−97.05255) |
| within_shape | service_road\|service_road | gs | 2 | 1.750 | — |
| within_shape | groundside_pavement\|groundside_pavement | gs | 2 | 1.080 | — |
| plane_gradient | apron\|apron | **airside** | 2 | 0.590 | — |
| runway_end_skirt | runway_clearance\|runway_clearance | **airside** | 1 | 1.580 | (32.91578,−97.02524) |
| mid_edge_step | service_junction\|service_junction | gs | 1 | 0.650 | — |
| plane_gradient | service_junction\|service_junction | gs | 1 | 0.110 | — |

Top-90 %: transverse service_road (178), within_shape apron (119),
within_shape junction (113), transverse service_junction (45),
transverse junction (31), within_shape service_junction (29) — 515 of 569.

### 2.2 The airside 284 splits in two, cleanly

| | rows | worst | class |
|---|---:|---:|---|
| **within 400 m of (32.87913,−97.05214)** — the 18L/36R south end | **134** | 21.74 m | ONE defect site, §2.4 |
| everywhere else | 150 | 1.58 m | the C1 apron family + marginalia |

Within 400 m: within_shape junction 109, transverse junction 21,
strip_seam_tear 2, mid_edge_step junction\|runway 2.
Elsewhere: within_shape apron 119, transverse junction 10, strip_arc 6,
within_shape junction 4, drainage_spine apron 4, transverse apron 4,
plane_gradient 2, runway_end_skirt 1.

The apron core is two sub-metre sites — 62 rows worst 1.050 m at
(32.90613,−97.04591) (`-10317`,`-11474`,`-12980`) and 48 rows worst 1.030 m at
(32.90004,−97.04295) (`-11476`,`-11477`) — i.e. **the C1 `within_shape::apron`
family the held route-metric ruling addresses**, at KDFW scale and sub-metre
magnitude.

### 2.3 The bridge refusal, and what a viewer will see

**My build reproduces the guard exactly** (`[object-bridge] KDFW: 0 bridge(s)
[none], 0 tunnel(s), 1 refused, 4 road network(s)`), writing a
`_CLASSIFICATION_CACHE_VERSION = 18` sidecar:

| | |
|---|---|
| resource | `220.obj` (the pack's pavement inset slab) |
| reason | `bridge_deck_scale_implausible` — DECK_CARRIED on `deck_profile_fallback` evidence, **length 2,849.6 m > 1,000 · width 820.6 m > 60 · area 263,160 m² > 40,000** |
| measured | crest 8.00 m, girder clearance 2.01 m, contract DECK_CARRIED |
| `has_measurable_deck` | **False** → no rigid deck-top seat (R12-2); the record falls to the **generic y-bake** |
| footprint (read from the pack's own `220.obj` VT extents at its DSF anchor −97.048995289, 32.887477588, heading 0.099°) | x 400.3…1222.0 m, z −2543.2…306.1 m → **lat 32.88473…32.91032, lon −97.04471…−97.03592** (span 821.7 × 2849.3 m — the record's own numbers) |
| 62 other object placements share that same DSF anchor | the LSGG shared-datum authoring class |

**What the terrain under it now does.** 7,714 emitted patch nodes lie inside
that footprint; our surface there runs **169.85 … 181.48 m** (airside pavement
173.19…178.62, range 4.7 m). The refused datum would have been **183.286 m**
(anchor DEM sample + 8 m) — 5 to 13 m **above** everything we grade there. The
refusal is doing real work: without it, 193 hard deck-end pins at 183.29 invert
the band across the whole eastern terminal core.

**The in-sim list (this is what gates the owner's bounds ratification).**

1. **KDFW `220.obj`** — the eastern terminal pavement inset (Terminal A/C side,
   lat 32.8847…32.9103). Expected: the inset renders on our solved surface with
   no plateau, no trench, no dug corridor. **The thing to look for**: the record
   is now on the *generic y-bake* with `has_measurable_deck=False`, i.e. **one
   rigid y for a 2,849 m × 821 m object over a surface that varies ~4.7 m on the
   pavement it covers**. If the bake picks a single ground sample, the slab can
   float or bury by up to a couple of metres at its north/south extremes. Check
   the two ends of the inset, not its middle.
2. **KMCI — 6 records**, all cosmetic road bridges refused by the *clearance*
   gate (girder clearance under `BRIDGE_ROAD_CLEARANCE_MINIMUM_M` = 4.2 m):
   `road-bridge-1.obj` (2.45 m), `road-bridge-10.obj` (2.09 m),
   `road-bridge-2.obj` (2.42 m), `road-bridge-7.obj`+`road-bridge-8.obj`
   (2.23 m), `road-bridge-6.obj` (2.29 m), `road-bridge-5.obj` (2.30 m).
   Expected: **no corridor is dug beneath them** — the ground runs at grade
   under the bridge and the object is y-baked onto it. Look for a road bridge
   sitting flush on the ground with no gap under the span.
   `road-bridge-4.obj` (TERRAIN_CARRIED, no clearance measured) is **not**
   refused and keeps its feature.
3. **KDEN — 4 records**, jetway/under-jetway-office assemblies refused on the
   same clearance gate at 3.51–3.52 m:
   `Under_Jetway_Office.obj`+`Bridge_Len_5/6`+`Rotunda`,
   `Under_Jetway_Office.obj`+`Bridge_Len_9`+`Bridge_Support_Office` (×2 records),
   `Under_Jetway_Office.obj`+`Bridge_Len_5/8/9`+`Bridge_Support_Office`.
   Expected: no corridor under the jetway; the office sits on grade.
   KDEN's 19 other bridge records (garage/parking/mid bridges, the
   `Train_Hider` deck, three 4.67 m-clearance jetway assemblies) are unaffected.
4. **OTHH — unaffected, as the spec requires**: all three real viaduct records
   expose no girder line (`clearance = None`, which refuses nothing) and none
   approaches the scale bounds. The fixture airport keeps its decks.

*Provenance note:* items 2–4 are the production predicate
`object_terrain_features.contract_refusal_reason` evaluated against the
**cached** classification records in the shared data repo (read-only; KMCI
`+39-095`, KDEN `+39-105`, OTHH `+25+051`). They are a prediction of what a v18
build will refuse, not an observation of one — the numbers match the
20260815g blast-radius claim (6 KMCI + 4 KDEN) exactly.

### 2.4 Do the 284 airside rows sit on the refused structure?

Rows bucketed by distance to the `220.obj` footprint box:

| bucket | airside | groundside |
|---|---:|---:|
| **inside the slab** | **56** | 89 |
| within 100 m | 21 | 17 |
| within 500 m | 57 | 29 |
| beyond 500 m | 150 | 150 |

So **134 of 284 airside rows (47 %) are on or beside the refused structure** —
but they are the ordinary apron/junction family (within_shape 50 inside +19+50,
transverse 5+1+1, plane_gradient 1, drainage_spine 1+3, strip_arc 3), worst
1.05 m. **No residual plateau, no deck-shaped defect, nothing over 1.1 m
anywhere inside the footprint.** The refusal is clean at the census level.

The 150 rows beyond 500 m are the ones that matter, and 134 of them are one
site — which is **not** the bridge:

**THE KDFW HEADLINE — an EAT anchor-rect pin contradiction of 21.2 m.**
The build's own lines:

```
[eat-anchor-rect] 72 node(s) over 6 crossing segment(s) pinned at the
                  departure-surface regulation value.
[eat-anchor-rect] KDFW: 5 of 72 pin(s) REFUSED over 1 of 5 rect(s) — NO taxi route
                  from any node of the rect to any runway anchor, so it is not an
                  end-around taxiway (a perimeter road in the corridor is claimed
                  by the rect's geometry and refused by its route); released to
                  their seed.
[eat-anchor-rect] KDFW: 3 of 67 pin(s) REFUSED — an EAT pin never contradicts a
                  senior hard runway/seam anchor within route budget; worst node
                  3314 pin 196.824 is 21.225 m past its ceiling 175.599 (witness
                  anchor 3311 = 175.290, route budget 0.3091 m); released to
                  their seed.
[writeback-band]  72 solved value(s) CLAMPED into the unified reach band
                  (48 floor-side, 24 ceiling-side); worst +21.49 m on junction/
                  — solved z′ 175.26 against band [196.75, 196.90].
[final reach band] 2 of 31575 EXAMINED vertex(es) OUTSIDE their band
                  (pinned=2; worst 20.8805 m) — band [196.824, 175.943] (INVERTED)
```

Runway 18L/36R's CIFP pins are `[(0.0, 183.4896), (1.0, 175.26)]`; the site's
emitted junction ways `-10253`/`-10254` span **174.99…196.82 m** — a 21.8 m
internal range on airside pavement, with `graded_strip -13245` stranded at
195.0…196.7 beside a runway at 174.96…183.19.

Both guards fired (8 of 72 pins refused) and the damage still shipped: the
refused pins are *released to their seed*, and **196.824 is still what authors
the band** the writeback then clamps 72 values into — an inverted band
`[196.824, 175.943]`. This is the phantom/false-EAT class the canon already
knows (RULINGS 2026-08-12 "the pending false-EAT ratification gains its third
guard candidate: an EAT pin with NO taxi route to any runway anchor is not an
end-around taxiway"; memory `eat-anchor-rect-next`, `kclt-step-reach…`, the VHHH
canyon "phantom-EAT pins author the band at the remaining ends and the seal
enforces them"). **KDFW is its first FAA-fixture instance and by far its
largest: 21.5 m, 134 airside rows, one site.**

---

## 3. Known-vs-new class split

### KAFW — 341 adjudicated

| class | rows | known? |
|---|---:|---|
| road-face chord exposure **over 8 %** (chord-limiter payable): transverse 13 + within_shape 23 | 36 | KNOWN — wave-3 step 1 |
| road cross-section **2 % transverse** debt under the 8 % chord cap: transverse 148 + within_shape 16 | 164 | **NEW** (N-1) |
| lot ring inherits its unlimited road welds | 72 | KNOWN — corollary of the same limiter gap (spec clause 3) |
| adjacent-ground band (`strip_arc` 24, `strip_longitudinal` 1) | 25 | KNOWN |
| `runway_crown` realisation residual | 34 | **NEW** (N-2) |
| apron↔building step marginalia (`mid_edge_step` 3 + `vertex_to_edge_step` 1) | 4 | KNOWN (C6) |
| road step marginalia (`mid_edge_step` service_junction\|service_road) | 2 | KNOWN (C6) |
| `transverse::junction` airside pair | 2 | KNOWN (C3 family) |
| `plane_gradient`, `lateral_contiguity` singletons | 2 | KNOWN (C6) |
| solver-exit / both-hard road pins (the *author* of the road+lot rows above, not a separate count) | — | **NEW** (N-3) |
| 4 mutually-infeasible tile-seam DEM pin-pairs | — | **NEW** (N-4, no rows yet) |

**KNOWN 143 rows (42 %), NEW 198 rows (58 %).** Total 341. Rows are counted
once, in the class that prices them; N-3 is the *mechanism* behind the road and
lot rows, so it carries no separate row count, and N-4 has minted no row yet.

### KDFW — 569 adjudicated

| class | rows | known? |
|---|---:|---|
| phantom/false-EAT pin contradiction at 18L/36R south — **interventionally proven, §4** | **134 airside** (139 incl. spill) | KNOWN class, ratification pending — **new instance, 21.5 m** (N-5) |
| C1 `within_shape::apron` (route-metric family) | 119 airside | KNOWN — held RM ruling |
| road-family transverse/within_shape (service_road 180, service_junction 74) | 254 gs | KNOWN + N-1 as at KAFW |
| `lateral_contiguity` | 14 gs | KNOWN |
| `mid_edge_step` road/junction | 14 gs | KNOWN |
| adjacent-ground band (`strip_arc`) | 6 airside | KNOWN |
| `drainage_spine::apron`, `transverse::apron`, `plane_gradient`, `runway_end_skirt` | 11 airside | KNOWN (C6 marginalia) |
| remaining junction/apron singletons | 17 | KNOWN |

**Nothing at KDFW needs a class no ruling covers** — its headline is a known
class at an unprecedented magnitude, and its groundside is KAFW's story again.

### The NEW classes, each with the ONE measurement that would confirm it

**N-1 — the road family's 2 % cross-section transverse is generated but does not
hold.** 164 KAFW rows (148 `transverse` + 16 `within_shape`; plus the same
family's bulk at KDFW) at 2–8 %, i.e. over
`SERVICE_ROAD_MAX_TRANSVERSE` (0.020) and under the road chord cap (0.080), on
pairs the solver *did* bake (`pair_caps` carries
`service_junction|service_junction` 4,845 and `service_road|service_road` 3,105
entries at KAFW, so this is not a lockstep-fork/domain gap). The generator
exists — `lateral_spine_nodes.insert_service_lateral_nodes` under
`config.SVC_SPINE_FIRST`, 4,575 KAFW cross-section nodes planted — and the
final projection still exits over cap on exactly this family.
**Measurement (one arm):** `O4_SVC_SPINE_FIRST=0 build_airport.py KAFW` — the
gate documents itself as restoring the previous behaviour byte-identically.
If the 148 rows are *unchanged*, the spine-first cross-section pairs are inert
at KAFW (planted but not binding) and the fix is in the binding, not the
planting; if they get much worse, the mechanism is a partial enforcement whose
residual is what we see. Either answer names the owner of the largest single
row class at both fixtures. *(Not run — arm budget spent on N-2 and N-5.)*

**N-2 — the runway-crown realisation residual.** 34 KAFW airside rows; the
emitted ring wobbles ±0.03–0.05 m about its declared 0.23 m crown relative to
the `crown_spine` breakline (median +0.013, sd 0.034, n=256), symmetric, not a
taper, and **not** the reader's ridge interpolation (corr −0.035) and **not**
emit decimation (arm 1, refuted: 34 → 34).
**Measurement (one arm):** `O4_FINAL_GRADE_PROJECTION=0` (or
`O4_FINAL_PROJECTION_LATE=0`) on KAFW, reading the same ring-vs-spine delta
distribution: the crown is applied inside the solve (`z′ = z + c`, writeback
subtracts `c`) while the final projection runs afterwards in uncrowned space —
if the residual's sd collapses with the late projection off, the projection is
moving ring and ridge by different amounts and the crown is its collateral;
if it does not, the author is the crown writeback itself.

**N-3 — the road family exits the final projection with 195 BOTH-HARD over-cap
edges.** KAFW's certificate: `final#1` ENTRY 1,023 → EXIT 326 over-cap law
edges, 195 both-hard, dominated by `unified:service_junction` (143) and
`unified:service_road` (72); plus 121 empty-polytope midpoints. Both-hard means
neither endpoint can move — a *pin* contradiction, which no limiter and no
convergence budget can pay.
**Measurement (no build):** the build already prints the family breakdown;
what is missing is *which authority pinned each endpoint*. Add nothing — run
the existing band instrument dump (`O4_BAND_INSTRUMENT_DUMP`, the report-only
instrument grafted default-on 2026-08-15) on a KAFW build and read
`layout._band_instrument_findings` for the 195 edges: it is built to "name each
absorbed contradiction with its floor/ceiling-minting anchors". That is one
existing default-on instrument, no new code, no A/B.

**N-4 — mutually infeasible tile-seam DEM pins.** KAFW `[seam-pins]`: 4 of 40
pin-pairs over the 1.5 % cap, worst 4.78 m excess (14.65 % over 36.4 m); KDFW
1 of 4 at 0.02 m. No census row is currently attributed to them, so this is a
*latent* class — recorded because hard DEM pins that contradict each other are
the same failure shape as N-5, and because a tile build (not a patch build) is
where they become visible.
**Measurement:** the KAFW `--tile 32 -98` arm, censused, comparing the seam
neighbourhood rows against this patch build. Cheap only as part of a tile round.

**N-5 — the refused EAT pin's *seed* still authors the band.** KDFW: 8 of 72
pins refused by the two guards, "released to their seed", and the seed value
196.824 is nonetheless the band floor the writeback clamps 72 values into,
producing an inverted band and 134 airside rows at up to 21.74 m. The *class*
(false/phantom EAT) is known and its guards are already awaiting owner
ratification; what is new is that **refusal is not sufficient** — releasing a
pin to a seed that carries the same wrong value leaves the same authority in
place one level down.
**Measurement (arm 2, RUN — see §4): CONFIRMED.**
`O4_EAT_SURFACE_CEILING=0` on KDFW takes airside **284 → 145** (−139), the
EAT site's 134 rows to **zero**, and the worst airside \|de\| from **21.74 m to
1.58 m**, at a cost of +7 groundside rows.

---

## 4. Arm 2 (spent) — `O4_EAT_SURFACE_CEILING=0` on KDFW: CONFIRMED

`O4_EAT_SURFACE_CEILING=0 build_airport.py KDFW --tag kdfw_noeat`, rc=0,
provenance `sha=47e83ed4 dirty=false`, one non-default gate
(`O4_EAT_SURFACE_CEILING=0`). The gate's own documentation: *"Gate OFF ⇒ no
per-end store, no pins, no verification reader — byte-identical to the
pre-feature build"* — so this is a clean subtraction of the whole feature.

| family / roles / side | base (EAT on) | arm (EAT off) | Δ |
|---|---:|---:|---:|
| within_shape junction\|junction **airside** | 113 | 4 | **−109** |
| transverse junction\|junction **airside** | 31 | 5 | **−26** |
| strip_seam_tear graded_strip **airside** | 2 | 0 | **−2** |
| mid_edge_step junction\|runway **airside** | 2 | 0 | **−2** |
| within_shape apron\|apron **airside** | 119 | 119 | 0 |
| transverse service_road groundside | 178 | 178 | 0 |
| transverse service_junction groundside | 45 | 48 | +3 |
| mid_edge_step service_junction\|service_road groundside | 13 | 17 | +4 |
| every other family | — | — | 0 |
| **totals** | **569 / 284a** | **437 / 145a** | **−132 / −139** |

* **Airside rows within 400 m of the EAT site: 134 → 0.**
* **Worst airside \|de\|: 21.740 m → 1.580 m** (the arm's worst airside rows are
  the pre-existing `runway_end_skirt` neighbourhood at (32.9157,−97.0252)).
* Writeback-band clamps: worst **+21.49 m → +0.82 m**; the inverted band is gone.
* `within_shape::apron` 119 is **byte-stable**, which independently confirms the
  C1 apron family and the EAT class are two different debts.
* Collateral: **+7 groundside** rows (transverse service_junction +3,
  mid_edge_step service_junction\|service_road +4) — the road family loses a
  boundary the pins had been holding. Small, and on the side the chord limiter
  owns.

**Attribution, interventional and unambiguous: the EAT anchor-rect feature
authors 139 of KDFW's 284 airside rows (49 %), and every airside row above
1.6 m.** N-5 is confirmed as stated: the guards fire (8 of 72 pins refused) and
the refusal is not sufficient, because a pin released to its seed leaves the
same value authoring the band.

This is *not* a recommendation to turn the gate off — the departure-surface
ceiling is real regulation and KDFW is one airport. It is the measurement the
pending false-EAT ratification was missing, and the size of the prize.

### 4.1 SM1/SM2/SM3 for KDFW's 119 apron rows — GATED, not skipped

`tools/sm_decompose.py` (promoted on lane/c3rework, `e5744c3`) calls
`grade_graph.route_pair_legs`, which **does not exist on main 47e83ed** — it
lives only on the held route-metric lane (`lane/routemetric` / its descendants,
`grade_graph.py:1966`). The decomposition is therefore **structurally
unavailable at the merge base**; running the promoted tool from the other
lane's tree would import that lane's `check_grade` and measure KDFW's patch in
a different law frame, which is the cross-tree defect. Reported as gated on the
RM merge rather than approximated.

---

## 5. What this dossier did NOT do

* No fix, no default change, no spec edit.
* No timing claim of any kind (both fixtures built alongside other lanes'
  builds; wall times are recorded by the harness and are not quoted as evidence).
* Two interventional arms only, as briefed: arm 1 (KAFW, `O4_EMIT_DECIMATE=0`)
  and arm 2 (KDFW, `O4_EAT_SURFACE_CEILING=0`). N-1 and N-2's arms are
  *proposed*, not run.
* Shared data repo untouched — both builds report
  `shared repo UNCHANGED by this build (full-surface before/after snapshot)`.
* No private build or census wrapper; no new tool. The one-off reads in this
  dossier import `check_grade` / `object_terrain_features` and read verbatim.
