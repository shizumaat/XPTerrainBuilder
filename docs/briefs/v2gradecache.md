# Brief pack — lane `v2gradecache`

Base: main `09102cdc` · generated 2026-09-13 by `tools/brief_pack.py`

Read THIS file first and whole. Do not grep the spec, RULINGS or INDEX monoliths for
what is here; use `tools/docq.py` for anything else you need.

## Task

VHHH's 44-minute planar stage: at_grade_geometry re-clipped and re-unioned each pack resource once per placement — memoize per (resource, plane), retain per resource, time it, refuse over budget (RULINGS 2026-09-13bp).

## The brief

The owner's +22+113 tile build spent 63 min; scout v2zgszunion (RULINGS 13bp — read it whole, it has the stack and numbers) found 2,626 s of it in `planar/basins.py:419`'s loop calling `airport/obj8.py at_grade_geometry` (its `unary_union` at :924 and `obj8_clip.py:158 _clip_both`) ONCE PER PLACEMENT for the same five custom pack resources (VHHH: 5,078 custom objects, 785 resources, 74 basin rings; ~0.9 s and ~25 MB retained per call). Implement 13bp (i)–(iv): memoize the clip+union per `(o.resolved, round(plane, 2))` in the resource's local frame and apply the placement affine afterwards; `grade_cache` / `cover_cache` retain per RESOURCE; a real timer `planar.basins.grade_geometry_s` (+ calls, vertices) in the report; a vertex-budget refusal naming the pack. The plane quantum is the ONE rounding that could move a basin rim reading — prove byte-identity of LEMD's and OTHH's basin rims by replay (`Ortho4XP/tools/v2_solve_replay.py` captures via `tools/harness/frames.py list LEMD` / `OTHH`; the planar stage `PYTHONPATH=src venv/bin/python -m auto_patch_v2.planar ICAO --out DIR` on both). VHHH: the scout's tracer log and ZGSZ patch are registered (`frames.py list VHHH ZGSZ`); the scout's worktree `v2zgszunion` may still be mounted — tear it down (`lane_worktree.sh down v2zgszunion`) before yours goes up if the name collides. Measure VHHH's planar wall and peak RSS with ONE harness build (`build_airport.py VHHH --no-ledger` is a TIMING run — foreground, exclusive of your own other work; `/usr/bin/time -l` for the RSS).

## Bars

- VHHH planar wall 2,626 s → ≤ 120 s; peak RSS ≤ 4 GB (`/usr/bin/time -l`); `at_grade_geometry` unions ≤ the number of distinct (resource, plane) pairs (report the count).
- LEMD and OTHH basin rims BYTE-IDENTICAL (planar replay on the registered captures; every basin record's rim ring, floor, ramp corridor unchanged).
- `planar.basins.grade_geometry_s` / calls / vertices printed in every report; the vertex-budget refusal names the pack and fires on a synthetic over-budget fixture (twin) and not on VHHH.
- ZGSZ / VMMC / CYXY byte-identical (stock-only packs never entered the loop).
- Suite twice (`tests/auto_patch_v2 tests/test_harness.py`).

## Files

Yours: `Ortho4XP/src/auto_patch_v2/airport/obj8.py`, `Ortho4XP/src/auto_patch_v2/airport/obj8_clip.py`, `Ortho4XP/src/auto_patch_v2/planar/basins.py`
Other lanes' (do not touch): `Ortho4XP/src/auto_patch_v2/airport/placement_family.py`, `Ortho4XP/src/auto_patch_v2/emit/osm_adapter.py`, `Ortho4XP/src/auto_patch_v2/planar/structure_underpass.py`

## RULINGS

## 2026-09-13bp — ATTRIBUTED (scout `v2zgszunion`, offline reproduction with a `union_all` tracer and a 10 GB RSS cap): 13bi's premise REFUTED — the +22+113 worker (pid 31256) was VHHH, not ZGSZ, and the PLANAR stage, not emit. The app's own `VHHH.report.json`: `took` 3,800.5 s of which `wall_s.planar` 2,626.3 s, emit 3.1 s, constraints 277.5, rebake_plan 201.3, verify 320.7; bank 0 rings (1,828 stations all immaterial); `seam_bands 0` (no graticule seam — the 13as seam-band code is not on the path). VMMC 16.1 s, ZGSZ 94.1 s (ZGSZ through the harness 97.6 s, optimal). THE CALL: `airport/obj8.py:924 unary_union(lines)` in `at_grade_geometry`, driven from `planar/basins.py:419` (`grade_cache[o.id] = obj8.at_grade_geometry(...)`) inside the loop over 74 basin rings, with `obj8_clip.py:158 _clip_both`'s inner union — 311 calls in 11 min (30–308 clipped-triangle LineStrings, 20k–136k vertices, mean 0.87 s, max 6.03 s, 270 s total), only FIVE distinct geometry signatures: FIVE pack resources re-clipped and re-unioned ONCE PER PLACEMENT because `grade_cache` is keyed on the placement (`o.id`) while `cache.geometry(o.resolved)` is per resource; extrapolated ≈ 2,700 unions ≈ the 2,626 s. Memory 2.2 → 10.0 GB in 7 min (~25 MB retained per placement in `grade_cache` / `cover_cache`); the app's worker reached 34.9 GB. WHY VHHH: `at_grade_geometry` returns at once for `lib/…` (stock) resources — ZGSZ 700/700 stock, VMMC 1,925/1,925 stock, VHHH (Taimodels) 5,708 placements with 5,078 CUSTOM objects across 785 resources and 74 basin rings. The snapping-noder frame in the owner's `sample` was one sample of thousands of ~1 s calls, not one pathological union (no exception logged; unverified as a fallback). `planar.basins.object_read_s` reads 6.8e-06 s while the loop burned ~2,400 s — UNTIMED, which is why it looked hung. RULED (Fable): (i) `at_grade_geometry`'s clip + union depend only on `(resource, plane)` — memoize per `(o.resolved, round(plane, 2))`, apply the placement affine afterwards (VHHH 2,700 → ≤ 785 unions, in practice tens); (ii) `grade_cache` / `cover_cache` retain one geometry per RESOURCE, not per placement; (iii) a real timer (`planar.basins.grade_geometry_s`, calls, vertices) and a vertex-budget REFUSAL that names the pack ("N custom objects, M vertices, T s") instead of a silent 44 min; (iv) bars: VHHH planar 2,626 s → ≤ 120 s and peak RSS ≤ 4 GB on the harness build; LEMD and OTHH basin rims BYTE-IDENTICAL (the plane quantum is the one rounding that could move a rim — prove it by replay); the timer in every report. Lane `v2gradecache` (pack).

## Tool: v2_solve_replay

| `Ortho4XP/tools/v2_solve_replay.py` | --why-vertex V [--why-relax FAMILY ...]`; `--why-hard [N]` on either a `--replay` arm or a `--why-from PKL`) | You are iterating on the v2 SOLVE (a law table, a generator, the shape stage, the planar build) and want the runway bows / z − DEM / joints / yielded-rows figures per change WITHOUT paying for the load stage each time — the synthetic-first solve arm (CLAUDE.md BUILD ECONOMY; `docs/specs/auto-patch-v2/heca-sag-ablation/ablate_heca_pin.py` was the scratch pattern, promoted on its second use by lane v2chord, RULINGS 2026-09-08d). `--capture` runs load → **the PACK PARTITION + GROUPS** (`build.py:288-318`, owner RULINGS 2026-09-11j — folded in 2026-09-12u by lane `v2padceiling` after scout `v2unsettled` measured the trap: a capture without them carries an `Airport` with no `partition` and no `groups`, so the replay silently solves a DIFFERENT problem — no `foot_rows`, no `pad_relief` targets, no basin bodies, and the shipped LEMD hard set could not be reproduced at all; a capture predating it is now REFUSED BY NAME at replay, `capture_has_groups`) → classify → planar (which labels the SHAPES, owner RULINGS 2026-09-08k) → flat site → road profile → shape stage exactly as `pipeline/build.py` and pickles the airport, the classification, the planar map and the stage (HECA 56 s; LEMD 232 s, of which 104 s partition); `--replay` resumes from the named stage under the CURRENT tree (constraints + joint filter + yield transform + ONE solve by default — 08k (4), no joint re-solve; `shapes` rebuilds the shapes over the captured map and re-runs the stage — for a change in `planar/shapes.py` or `[terrace]`; `planar` the whole map), and prints per two-threshold runway the crown-ridge BOW, the ridge minimum, z − DEM mean/min/max, the law-tier line, `yielded_rows` per family and the max apron grade PER SHAPE with the steepest rows per family (face, grade, span, lat/lon), the shapes (count, the largest by vertices), the joints (count, gap joints, max built step, the worst with their shape pair), the road steps, and per `--site` the vertices within `--site-radius` (z − DEM, roles, shape ids, the max |dz| over a short edge = a step). `--emit DIR` writes the arm's patch through the build's own emit half so `patch_proximity_diff.py` and `undulation.py` can read a same-frame divergence on a replay arm; `--verify` runs the v2 VERIFY CENSUS on the solved surface and prints the rows per family and the DEFECT families the app's driver gates on (lane v2smooth round 2, RULINGS 2026-09-08v — a weight sweep needs the defect count per arm without a 130 s build); `--design-weight TERM=W` overrides one `emit.toml [design]` weight for the arm (the measurement arm, never a default). `--drop-generator` also accepts the pseudo-generator **`eat_ramp_reach`** (lane `v2eatramp`, spec §36 (5)): the EAT ramp's trend WITHDRAWAL is a channel edit made before the solve, not a row, so it cannot be dropped by the row filter — `eat_anchor_rect` drops the pins and their reach together, `eat_ramp_reach` the withdrawal alone (the §36 (5) before-arm). Lane v2shapes (2026-09-08): HECA capture 56 s; one pass 144–150 s (the 08g three-pass law read 357 s). `--why-from PKL --why-hump RUNWAY S0 S1` (the `solve.why` bindings and chain trace on the highest ridge vertex above the chord, from a duals solve of the SAME LP — kept out of the timed arm) with `--why-relax FAMILY ...` (relax-one-family arms: the hump's dz after a full re-solve without the family — the INTERVENTIONAL holder read). `--why-hard [N]` lists EVERY VIOLATED HARD ROW of the solved surface — generator, ruling, demanded vs allowed metres, and per vertex the id, coefficient, z, DEM, lat/lon and roles — re-assembling the design problem with `solve.design.assemble` and applying design.py's own `2 / Σ|c|` row scaling, so every violation reads in the METRES `hard_tol_m` is stated in (promoted from scout `v2unsettled2`'s `hardrows.py` on its second use, spec §32 (4), RULINGS 2026-09-13ac: `HARD SET NOT SETTLED` names ONE truncated ruling, which cost 12u a scout re-deriving the population by hand and 13ac needed the row's VERTICES to see that main's worst row was a pair the zone projection had clamped independently). Twins: `tests/auto_patch_v2/test_v2padceiling.py` (the capture calls every pre-solve stage `pipeline/build.build` calls, read off both sources; the refusal predicate) — the rest of the replay is an instrument over `pipeline/build.py`'s own stages, which the pipeline twins cover. |

## Tool: planar/__main__

| `Ortho4XP/src/auto_patch_v2/planar/__main__.py` | **`--stage structures` (lane v2lemd4, 2026-09-06): the STRUCTURE REPLAY** — stop after the structure stages (objects read, tunnel-object corridors, tunnels, basins) and write `structures.json` with every corridor / tunnel / basin record and every refusal by name (rim protrusions, through-grade shells, edge-wall readings, decks, the mouth ground, the design grade), no arrangement, no solve: ~25 s at LEMD against a 160 s build — the synthetic-first instrument for a site attributed to a pack OBJECT (LEMD 970 / 1088 were iterated on it five times before the one closing build). Otherwise: You need the auto-patch-v2 PLANAR MAP of an airport (M1, `docs/specs/auto-patch-v2/m1-report.md`): loads the airport through the v2 loaders (apt.dat from the pack v1 would pick, CIFP, the three OSM feeds, the tile DEM + inset, the DSFTool dump cache — all READ-ONLY on the shared corpus, the same mounts the harness resolves), classifies every pavement cell (`classify/rules.toml`), builds the ONE noded arrangement (faces/edges/vertices/breaklines, I1-I7 validated, zero T-vertices, vertices on the 0.5 m identity grid) and writes `faces.geojson`, `breaklines.geojson`, `report.json` (counts, wall time, load report, cells by role). It is a PRODUCER, not a build: no elevations are solved, nothing is emitted for the mesh, nothing is censused — the v1 census stays the oracle (`census.py` on the v1 patch until M2's `emit/osm_adapter` exists). Defaults come from the engine tree's mounts and its `Ortho4XP.cfg` `custom_scenery_dir`/`cifp_data_path`; override with `--xplane-root`, `--cifp-dir`, `--data-root`. Twins: `tests/auto_patch_v2/test_{airport_load,classify,planar,role_agreement}.py`. **`PROBE` table (lane v2corridor round 4, RULINGS 2026-09-10ab): with `--stage structures`, one row per Law C wall-corridor CANDIDATE with the two physical discriminators MEASURED — the nearest mouth road within `corridor_road_level_m` and its LEVEL there (the core's own longitudinal clamp; the DEM where the core levels nothing) against the corridor's floor, and whether a horizontal FLOOR SLAB spans the walls at floor depth. Measurement only (`read_wall_corridors(measure=True)`): neither reads in law, neither separates LEMD from OTHH (spec §12c), and a build pays nothing for them.** | **`--kml OUT.kml` (lane v2wallcorridor, 2026-09-08): with `--stage structures`, the same readings as a KML the owner reads — Law C wall corridors by class (level / bay / garage_ramp: trench ring, axis, mouth, widths, depths, authored grades, headroom, refusals), tunnel wall objects, tunnels built, door wells, sunken roads, basins. The RULINGS 2026-09-08n inventory-before-cut instrument.** **THE WALL PLATES AND EVERY REFUSAL (lane `v2wallplate`, spec §33, RULINGS 2026-09-13i): `structures.json` now also carries `plates` / `plate_refused` / `plate_stats` (the THIN-PLATE wall class — a pack object spanning `[tunnel.object] thin_plate_min_m`..`least_skirt` over a mapped bore or deck, with the object's own box as the corridor), `plate_mouths` (the OSM mouths a plate moved to its object's end) and `crest_from_approach` (the mouths whose DEM sample stood on an overbridge embankment); the KML gains a folder for each, plus `tunnel objects refused` — the four suppressed refusal prefixes are gone, so EVERY screened resource is named with its verdict (LEMD 105 -> 192 refusals over 182 screened resources).** ; each basin row carries its RIM RING and region in lat/lon (`rim_ll` / `region_ll`) and its ramp corridors (`ramp_rings_ll`, lane v2basinfoot 2026-09-13) so a dry run can answer "how far did this rim MOVE" and "what did the ramp reader admit or refuse", not just an area: ~25 s at LEMD against a 160 s build — the synthetic-first instrument for a site attributed to a pack OBJECT (LEMD 970 / 1088 were iterated on it five times before the one closing build). Otherwise: You need the auto-patch-v2 PLANAR MAP of an airport (M1, `docs/specs/auto-patch-v2/m1-report.md`): loads the airport through the v2 loaders (apt.dat from the pack v1 would pick, CIFP, the three OSM feeds, the tile DEM + inset, the DSFTool dump cache — all READ-ONLY on the shared corpus, the same mounts the harness resolves), classifies every pavement cell (`classify/rules.toml`), builds the ONE noded arrangement (faces/edges/vertices/breaklines, I1-I7 validated, zero T-vertices, vertices on the 0.5 m identity grid) and writes `faces.geojson`, `breaklines.geojson`, `report.json` (counts, wall time, load report, cells by role). It is a PRODUCER, not a build: no elevations are solved, nothing is emitted for the mesh, nothing is censused — the v1 census stays the oracle (`census.py` on the v1 patch until M2's `emit/osm_adapter` exists). Defaults come from the engine tree's mounts and its `Ortho4XP.cfg` `custom_scenery_dir`/`cifp_data_path`; override with `--xplane-root`, `--cifp-dir`, `--data-root`. Twins: `tests/auto_patch_v2/test_{airport_load,classify,planar,role_agreement}.py`. **`PROBE` table (lane v2corridor round 4, RULINGS 2026-09-10ab): with `--stage structures`, one row per Law C wall-corridor CANDIDATE with the two physical discriminators MEASURED — the nearest mouth road within `corridor_road_level_m` and its LEVEL there (the core's own longitudinal clamp; the DEM where the core levels nothing) against the corridor's floor, and whether a horizontal FLOOR SLAB spans the walls at floor depth. Measurement only (`read_wall_corridors(measure=True)`): neither reads in law, neither separates LEMD from OTHH (spec §12c), and a build pays nothing for them.** | **`--kml OUT.kml` (lane v2wallcorridor, 2026-09-08): with `--stage structures`, the same readings as a KML the owner reads — Law C wall corridors by class (level / bay / garage_ramp: trench ring, axis, mouth, widths, depths, authored grades, headroom, refusals), tunnel wall objects, tunnels built, door wells, sunken roads, basins. The RULINGS 2026-09-08n inventory-before-cut instrument.** |

## Registered frames: VHHH

VHHH  capture  base dc5517c0c156603c7b3339b2a9d80b21ccbc0caa lane v2zgszunion      2026-09-13T16:07:22  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/vhhh.35993.log  — shapely union_all tracer log; VHHH build aborted at 10 GB RSS inside planar/build build_basins -> obj8.at_grade_geometry; 311 unions / 270.5 s in 11 min

## Registered frames: ZGSZ

ZGSZ  patch    base dc5517c0c156603c7b3339b2a9d80b21ccbc0caa lane v2zgszunion      2026-09-13T16:07:14  /tmp/harness/v2zgszunion.osm  — ZGSZ standalone patch build, 97.6s rc=0, shared repo UNCHANGED — REFUTES the ZGSZ-emit premise of RULINGS 13bi

## Registered frames: LEMD

LEMD  capture  base ec8723e9   lane v2roadcap        2026-09-13T12:06:34  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/rw/cap/LEMD.pkl  — the v2roadcap-era LEMD capture used by scout v2unsettled2
LEMD  capture  base 864e7577   lane v2settle         2026-09-13T12:06:34  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/v2settle  — fresh main capture + per-law arms + logs (13ak)

## Registered frames: OTHH

OTHH  rebake   base 05050624   lane v2unboxed        2026-09-13T12:06:34  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/unboxed  — KCLT 1.0.324 / LEMD 1.0.325 / OTHH 1.0.326 rebake frames + dry arms

## Standing discipline (unchanged for every lane; the long form is `.claude/agents/lane.md`)

- Worktree: `tools/harness/lane_worktree.sh up <lane> <base sha>`; branch `claude/<lane>`.
  `git merge main` FIRST if main has moved past the base sha.
- `Ortho4XP/venv/bin/python tools/blast.py <file>` before editing each source
  file; files under 1,000 lines.
- ONE closing build through the harness (v2 is the only engine, RULINGS
  2026-09-13au — there is no `--engine` flag); base arms cut with
  `git archive <sha> src`, never another live checkout.
- NEVER write `/Users/noah/XPTerrainBuilderData` or `/Users/noah/X-Plane 12/Custom Scenery/`;
  no `--refresh-data`; every run prints `[guard] shared repo UNCHANGED`;
  lane-local `O4_DSF_CACHE_DIR` / `O4_AIRPORT_MOD_CACHE_DIR` under your scratchpad.
- Any wait loop is bounded (`timeout N` or `$SECONDS`); prefer foreground.
- Suite from `Ortho4XP/` twice: `venv/bin/python -m pytest -q --no-header -p no:cacheprovider -rfE tests/auto_patch_v2 tests/test_harness.py tests/test_role_edge_census.py`.
- Attempt cap two per rule; a bar that moves backwards twice → delete the code, report the measurement.
- Consumer census (RULINGS 2026-08-30l) in the spec MEASURED block before any consumer is edited.
- Extend tools, never fork; INDEX row + twin for any new option. Do NOT merge into main; do NOT write RULINGS.
- Read law with `tools/docq.py spec '§N'`, `tools/docq.py ruling 13xx`, `tools/docq.py index <name>`;
  find captures with `tools/harness/frames.py list [ICAO]`; register yours when done.
- REPORT: branch + sha; per-bar before → after with the frame named; what you did NOT do;
  intent questions with their measurement; files touched.

