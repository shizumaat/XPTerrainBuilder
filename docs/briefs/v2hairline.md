# Brief pack — lane `v2hairline`

Base: main `6f3f0110` · generated 2026-09-13 by `tools/brief_pack.py`

Read THIS file first and whole. Do not grep the spec, RULINGS or INDEX monoliths for
what is here; use `tools/docq.py` for anything else you need.

## Task

§39 THE HAIRLINE LAW: emit-side snap onto foreign edges, the hairline_pair census family, the mesh pre-flight, and the LEMD cause named — the LEMD tile would not load (2.3 M sliver triangles).

## The brief

The owner's LEMD tile from app 1.0.327 would not load in X-Plane. Scout `v2lemdload` (RULINGS 13bk) measured 2,301,676 triangles under 0.1 m² in the LEMD bbox (73 % of the tile), all in two clusters at LEMD's retention basins, where an emitted v2 patch pavement ring (marker 15) runs 0.06–0.26 mm from and parallel to the OSM water edge (marker 1). It is the same class as SPLP's seam fan (13an). Implement §39 (1)–(4) in that order: (1) the emit-side snap at the ring writer (find the one site every face's ring passes through before `write_tile_pieces` — `emit/osm_adapter.py` / the planar map's vertex identity); (2) the `hairline_pair` census family in `tools/check_grade.py LAW_FAMILIES` + `law/families.toml` (cockpit class: CRITICAL unconditionally) with a twin in `tests/test_harness.py`; (3) the mesh pre-flight in `Ortho4XP/src/O4_Mesh_Utils.py` before the Triangle4XP call (read the `.poly` segment list; refuse by name) and `Ortho4XP/tools/mesh_region_tris.py --hairline-audit` (promote the scout's reader: `/private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/nearpar.py`; INDEX row; twin); (4) bisect the cause on the LEMD capture: `tools/harness/frames.py list LEMD` (v2settle's fresh main capture at 864e7577 and v2roadcap's at ec8723e9 — the break is between 1770 and 1773 = between main ~ec8723e9 and 95c78040; replay the emit under `git archive` trees of the batch's merge commits: v2seampin d56d02e9, v2zerocrater e9db9207, v2roadcap eae16d6f, v2basinfoot 6f2e31d4 … and read the ring at 40.4762773, −3.5456742 against the water edge). The scouts' hairline readers (`nearpar.py`, `hairline_tile.py`, `poly_probe.py`) are in the scratchpad above.

Measurement: the LEMD MESH needs a TILE build (the airport entry emits the patch only): `tools/harness/build_airport.py LEMD --tile 40 -4` in your lane worktree (lane-local caches; ~10 min; the 13as-era DEM-prep write of `index.json` was guard-refused with a degraded frame — quote the guard line if it recurs), then `mesh_region_tris.py --bbox 40.44,40.55,-3.62,-3.50 --area-bands --aspect`. Before that, the patch-side census (`hairline_pair`) on the harness LEMD patch is the cheap bar. Do not touch the app's shipped tiles or the data repo.

## Bars

- LEMD tile: sub-0.1 m² triangles 2,301,676 → low hundreds; aspect p50 7,549 → < 5; DSF ≤ 23 MB; triangles ≈ 2.93 M (1.50.1770's count).
- `hairline_pair` 0 on LEMD, KCLT, SPLP, CYXY patches on the lane arm; > 0 on the LEMD control (the instrument proves itself).
- The mesh pre-flight refuses a synthetic hairline `.poly` (twin) and passes the four tiles' real `.poly` files.
- The LEMD cause named (merge sha, file:line) with the ring-vs-water gap before → after at 40.4762773, −3.5456742.
- F-6, the basin ring, the ramps, the seam pins byte-identical where they are not the cause; SPLP seam bars (13as) hold.
- Suite twice (`tests/auto_patch_v2 tests/test_harness.py tests/test_role_edge_census.py tests/test_mesh_region_tris*.py` if present).

## Files

Yours: `Ortho4XP/src/auto_patch_v2/emit/osm_adapter.py`, `Ortho4XP/src/O4_Mesh_Utils.py`, `Ortho4XP/tools/mesh_region_tris.py`, `Ortho4XP/tools/check_grade.py`, `Ortho4XP/src/auto_patch_v2/law/families.toml`
Other lanes' (do not touch): `Ortho4XP/src/auto_patch_v2/airport/placement_family.py`, `Ortho4XP/src/auto_patch_v2/constraints/pads.py`, `Ortho4XP/src/O4_Cfg_Vars.py`

## Spec (design-surface) §39

## §39 THE HAIRLINE LAW (Fable 2026-09-13; RULINGS 2026-09-13an / 13bk; owner reads SPLP 13ag, LEMD 13bi, KCLT 13bj) — lane `v2hairline`

Three sightings in one day of one class: a constrained edge laid within
millimetres of, and parallel to, another — the SPLP bank chain 2.37 cm from
the meridian (16,298 Triangle4XP nodes in 1.92 m), LEMD's two patch pavement
rings 0.06–0.26 mm from the retention basins' water edges (2.30 M sub-0.1 m²
triangles, 73 % of the tile, X-Plane stalled), and (pending the scout) KCLT's
tearing. Triangle4XP must recover both segments and fills the wedge with a
Steiner cascade; GEOS's OverlayNG falls back to its snapping noder on the
same inputs (13bi's 35 GB union is the suspect twin).

1. **NO EDGE BESIDE ANOTHER.** An emitted ring vertex or edge may not lie
   within `identity.min_distinct_spacing_m` (0.5 m) of a FOREIGN constrained
   edge — an OSM water edge, a tile border, a core road ribbon, another
   face's ring — unless it SHARES that edge's vertices exactly (the 11-dp
   identity join). A ring edge within the spacing of a water edge is
   SNAPPED onto the water edge's own vertices (the shore weld: water is a
   datum and already carries the vertex), never laid beside it. Applied at
   ONE site: the emitter's ring writer, after every face is final.
2. **THE CENSUS FAMILY `hairline_pair`** prices every emitted ring edge
   against every foreign constrained edge of the tile's vector map within
   the spacing and within 5° of parallel — CRITICAL unconditionally (a
   load-time and texture defect, not a height).
3. **THE MESH PRE-FLIGHT.** `O4_Mesh_Utils` audits the assembled `.poly`
   before Triangle4XP: any non-adjacent constrained pair within the spacing
   and within 5° of parallel is REFUSED by name (pair, markers, coordinates)
   — the tile fails in seconds, never after a 40-minute Triangle run.
   `mesh_region_tris.py --hairline-audit` is the instrument (promoted from
   the scouts' `nearpar.py`).
4. **THE LEMD CAUSE IS NAMED AT ITS SITE.** Bisect the 1773 batch on the
   LEMD capture for the pass that moved the pavement ring onto the water
   edge (the shore weld count fell 403,636 → 145,257): the seam-band and
   bank-coverage changes (13as), the level belt (13w), the bank cut (§37
   (3)), the basin footprint (§24 (4)) are the suspects, in that order.

BARS: LEMD (`Data+40-004.mesh` from ONE harness tile build or the app's):
sub-0.1 m² triangles 2,301,676 → low hundreds, aspect p50 7,549 → < 5, DSF
≤ 23 MB, triangles ≈ 2.93 M; `hairline_pair` 0 at LEMD, KCLT, SPLP, CYXY
and > 0 on the LEMD control; the pre-flight refuses a synthetic hairline
`.poly` and passes the four tiles; F-6, the basin, the ramps byte-identical
where they are not the cause; suite twice.

## RULINGS

## 2026-09-13bk — ATTRIBUTED (scout `v2lemdload`, the shipped +40-004 tile read-only) and RULED (Fable, spec §39): LEMD's X-Plane load STALLED ON TERRAIN, not objects. `mesh_region_tris.py --area-bands --aspect` on `Data+40-004.mesh`: tile 3,149,290 triangles, 2,555,592 inside the LEMD bbox (81 %), of which **2,301,676 under 0.1 m²** (90 %) carrying 16,058 m² of ground between them (a 127 m square's worth), aspect p50 7,549 (CYXY 1.75, SPJC 2.36, the tool's HECA baseline 2.04), needles ≥ 20: 1,625,081 (64 %); by attribute 824,843 of them WATER (93.5 % of every water triangle in the tile). Build-to-build (1.50.1770 → 1773): triangles 2,930,290 → 3,149,290 (+7.5 %), interior boundary edges 1.31 M → 1.60 M, DSF 22.0 → 28.5 MB (+29.6 %), point pools 286 → 492, "Water is a datum" shore vertices 403,636 → 145,257, patch coverage 860,085 / 1,699,764 INTERP_ALT tris → 350,622 / 645,878; build wall unchanged (~9 m 50). THE CLASS: 13an's — the slivers sit in TWO clusters, A ≈ 40.4665–40.4680, −3.5485 … −3.5465 (~1.10 M) and B ≈ 40.4760–40.4785, −3.5470 … −3.5440 (~1.20 M) — LEMD's two retention basins — where an emitted v2 PATCH PAVEMENT RING (marker 15, `PATCH_RING_MARKER`) runs 0.06–0.26 mm from and exactly PARALLEL to the OSM WATER-BODY EDGE (marker 1): tile-wide 285 constrained pairs under 50 mm, 25 exactly parallel, the worst (0.0594 mm over 24.8 / 74.5 m at 40.4762773, −3.5456742; 0.0905 mm at 40.4767827, −3.5442805; 0.1146 mm at 40.4777080, −3.5462000 …) all in cluster B at 0.000°; cluster A carries four 0.08 mm constrained segments and near-duplicate nodes. Control CYXY: 67 pairs, minimum 0.8755 mm, no cluster. The water input is unchanged (Aug 24; the road clamp line byte-identical across eleven builds) — THE PATCH RING MOVED in the 1773 batch. F-6, the bank chains and the road ramps are CLEAN. Objects: 2,780 `OBJECT_DEF` / 5,043 placements, 0 duplicates, 0 NaN; 2,317 body files parse (162 MB, 2.2 M vertices), 0 failures — a real serial load cost, not a fault. No X-Plane crash report; `Log.txt` overwritten by the owner's 15:37 session (unverified). KCLT (13bj items 2, 5 and the owner's new coordinate 35.2056385, −80.9478669 — "also tearing") read as the same class pending the scout.

* FABLE'S RULING, spec §39 THE HAIRLINE LAW (one law for 13an, 13bk, and the ZGSZ union of 13bi if the scout confirms it): (1) EMIT-SIDE — no emitted ring vertex or edge may lie within `identity.min_distinct_spacing_m` (0.5 m) of a foreign constrained edge (an OSM water edge, a tile border, a road ribbon, another patch ring of a different face) unless it SHARES that edge's vertices exactly (the 11-dp identity join, `canonical-identity-join`): a ring edge within the spacing of a water edge is SNAPPED ONTO the water edge's own vertices (the shore weld — "water is a datum" already carries the vertex), never laid beside it; the census family `hairline_pair` (patch-side: emitted ring edge vs every foreign constrained edge of the tile's vector map within the spacing and within 5° of parallel) prices it, CRITICAL unconditionally (it is a load-time and a texture defect, not a height). (2) MESH-SIDE PRE-FLIGHT — `O4_Mesh_Utils` audits the `.poly` before Triangle4XP: any non-adjacent constrained pair within `min_distinct_spacing_m` and within 5° of parallel is REFUSED by name (the pair, the markers, the coordinates) — the tile build fails before a 40-minute Triangle run or a 2.3 M-triangle DSF; `mesh_region_tris.py --hairline-audit` is the promoted instrument (the scout's `nearpar.py`, its second use after 13an's). (3) THE LEMD CAUSE named at its derivation site — which emit pass moved the pavement ring onto the water edge in this batch (the "Water is a datum" shore weld count fell 403,636 → 145,257: the seam-band / bank coverage changes of 13as, the level belt of 13w, or the §37 (3) bank cut are the suspects) — by bisecting the batch's merges on the LEMD capture. Lane `v2hairline` (pack). BARS: LEMD sub-0.1 m² triangles 2,301,676 → low hundreds (CYXY's class), aspect p50 → < 5, DSF ≤ 23 MB, load-time triangles back to ~2.93 M; `hairline_pair` 0 on LEMD, KCLT, SPLP, CYXY; the mesh pre-flight refuses a synthetic hairline `.poly` and passes the four tiles; the F-6 / basin / ramp geometry byte-identical where it is not the cause.

## 2026-09-13an — ATTRIBUTED (scout `v2splpmesh`, from the owner's shipped products only, no build): the SPLP texture tear is a GEOMETRY CRACK made by the BANK, not a texture seam. 13am's suspect (the east piece's coverage edge at +5.02 m beside the border) is REFUTED — both tiles carry that pair, 5 m apart, harmless. The culprit: `Data-13-077.poly` (Triangle4XP's input, `O4_File_Names.py:405`) carries a `PATCH_RING_MARKER` (m=15) segment at lon −76.999999782 — **0.0237 m** east of and exactly parallel to the tile-border segment — which is `bank_foot` way −10046 (`bank:2`, nodes −2472/−2473) of `Patches/-20-080/-13-077/SPLP_auto.patch.osm`; two more bank chains (`bank:0` at 0.0275 m, `bank:1` at 0.0248 m) hug the same line. Triangle's `-Y` forbids Steiner points on the OUTER boundary but not on interior segments: every candidate on the bank segment encroaches the border 2.37 cm away, so Triangle splits the bank segment instead, 16,298 times (a 5.32 m segment, its northern 1.92 m at 0.118 mm pitch; 32,600 triangles touch the fan, 23,994 under 1e-5 m², ~8,600 needles 0.118 mm × 5–9.8 m) — the `emit/bank.py:19-27` class thought retired with the level rings, re-created by the seam. WHY THE FOOT IS THERE: `planar/overlay.py:305-329 seam_bands` buffers the sampled meridian by 5.0 m but the band lands OFF-CENTRE in the emitted frame (+5.0228 / −4.9754 m: centre +0.0237 m east — provenance unconfirmed; the `pyproj` tmerc round-trip in `model/frame.py:59-75` is the remaining candidate, the offset growing smoothly 0.0237 → 0.0287 m over 1.1 km of latitude); `emit/bank.py:792` closes the slit with `cov.buffer(bank_min_width_m = 5.0)`, and the two collars' inner edges land at +0.0229 / +0.0245 m — a HAIRLINE CRACK ~1.6 mm wide at +0.0237 m, whose boundary is the chain the bank foot follows; `emit/osm_adapter.py:341-345 write_tile_pieces` splits breaklines by `floor(lon)` with no seam-band test and writes the chain verbatim into the east piece. WHY ONE-SIDED: the crack's both walls fall east of the meridian (the west collar overshoots), so tile −13−077 gets it and −13−078's nearest foot node is 2.60 m away. IN THE DSF (quantum ~4.98 m lon / 0.1055 m lat) the fan collapses onto the border as 20 distinct vertices + 46 exactly-degenerate triangles; across the 1.92 m span the east DSF seam polyline carries 22 vertices and the west ONE 53.99 m straight edge — worst vertical gap 0.167 m at lat −12.1609617, 15.5 m from the owner's clicked node: THE TEAR. 13am's "border sound, mean |Δz| 0.011" compared COINCIDENT vertices and could not see it; polyline-vs-polyline over the whole meridian (n 4,617): 126 gaps > 0.10 m, max 4.106 m at lat −12.184588 (the §38 seam-residual berm). Swept all 90 emitted v2 patch pieces: exactly ONE node within 0.5 m of an integer graticule line — this one; SPLP is the only two-tile airport today, so the class is latent at every future seam-straddler with a ~1.6 mm margin.

* FABLE'S RULING, folded into lane `v2seampin` (§38 (3) as amended): (a) `emit/bank.py:792` unions the seam bands into the coverage BEFORE the min-width collar and differences them out of the banked region after — no foot vertex within `half_width_m` of a graticule line whether or not the band is centred; (b) `planar/overlay.py seam_bands` pins the band edges SYMMETRICALLY about the graticule line in the emitted lon (±`half_width_m` exactly, not a buffer of a sampled line through the frame round-trip) — bar: coverage edges at ±5.000 ± 0.01 m; (c) `write_tile_pieces` REFUSES (not drops) any breakline vertex inside a seam band; (d) the `seam_residual` census compares the two pieces' border POLYLINES (interpolated), not coincident vertices — bar: no gap > 0.05 m on runway/taxi roles, > 0.5 m elsewhere, across the whole meridian (today 126 > 0.10, max 4.106); (e) `bank_min_width_m` and `seam.half_width_m` cease to be independent: one is derived from the other or their relation is asserted at law-load. Mesh-side belt-and-braces (owed, not this lane): a pre-flight in `O4_Mesh_Utils` refusing any `.poly` segment parallel to and within `identity.min_distinct_spacing_m` of the outer boundary (unmeshable by construction under `-Y`). The lane may run ONE `--tile -13 -77` SPLP tile build (~10 min) as the interventional arm: bar — mesh vertices within 1 m of lon −77 on tile −13−077 18,638 → ≤ 2,500 and 0 triangles under 1e-5 m² at the site.

## 2026-09-13as — v2seampin REPORTED (lane dbc692ee off b78f8f32; merge waits on its LEMD byte-identity replay) and RULED on its deviations (Fable, §38 (2)/(3) and 13an (b)/(d) amended). SPLP, each piece alone then combined: (1) `Pin` + the seam pass DELETED + all 150 pins published → `seam: 150/150 on the DEM, 0 residual` — and exactly §38 (2)'s contradiction minted: the runway QP and its relaxation LP came back INFEASIBLE, because `solve/project.py`'s coupling test (`coupled = n_free < n_all`) counted nonzeros in the REDUCED matrix where a fixed vertex has no column, so 660 rows footed on two seam pins read self-contained and the elastic arm had nothing to relax — fixed to read the row's own terms (without it the runway rows ship uncertified; LEMD's threshold-footed rows are the byte-identity question); (2) seam KNOTS in the runway chord + the zone-band yield: optimal (elastic), verify 298 → 230 with the coupling fix; knots diagnostic: verify 298 vs 230, `airside_no_step` 81 vs 48, leader move 0.190 vs 0.072 — they cost fit to the changed target (RMS 0.304 → 0.366) and improve |z−DEM| 0.541 → 0.471; (3) bank cut by the seam band + explicit coverage union: bank foot within `half_width_m` 2 → 0 (closest 1.79 → 12.09 m), verify 226. BARS: seam 149/149 on the DEM; runway band-edge z−DEM +0.51 … +0.63 → 0.000; vertex 1408 51.07 → 54.50; `seam_residual` 106 rows max 3.433 → 0, `bank_across_seam` 2 → 0 (both new families in `LAW_FAMILIES`, twinned); cockpit CRITICAL motion 32 → 0 (the base priced against the published pins: 31 `seam_residual [runway|runway]` rows, worst 0.629 at −12.1637725, −76.9999539), visual 0 → 0; build wall 23.95 → 9.31 s (the 7-pass seam loop gone); MESH (tile −13−077, ONE `--tile -13 -77` build, DEGRADED frame — the engine's DEM prep tried to rewrite `Elevation_data/-20-080/S13W077_airport_insets/index.json` and the guard BLOCKED it, rc 1, no `--refresh-data`): border nodes within 1 m of lon −77 18,638 → 2,282, densest 1.92 m window 16,298 → 3 — the fan is gone with the segment. 13an (a) as pieces-cut + explicit union; (c) `write_tile_pieces` REFUSES a breakline vertex inside the band; (e) `law/model.py` refuses `bank_min_width_m < seam.half_width_m` by name. Patch-side polyline gap: 39 gaps > 0.10 m max 2.878 → 25, max 0.360, worst rolled-on 0.727 → 0.251. CYXY and KCLT dry replays BYTE-IDENTICAL; LEMD pending. Suite 1,222/0 twice.

* FABLE'S RULINGS on the deviations. (i) "Soft escalation group" is a NO-OP for `zones.adjacent_ground` (the design solve reads `soft` only for `[design] hard_rulings` heads, `solve/design.py:382`) — what landed is right: the one-way law's own withdrawal (a row governing a fixed vertex can only pull the pavement it was written to follow — the `water_exempt` clause), every yielded row NAMED (67 yielded, 46 unmet, worst 3.609 m at pin 1408: demanded +2.502, allowed −2.183 … −1.107). §38 (2) reads so. (ii) 13an (b)'s ±5.000 ± 0.01 m is UNREACHABLE and withdrawn: the tmerc round trip is refuted (a Newton correction changed nothing); the residue (+5.016 / −4.973) is the arrangement's own `unary_union(grid_size = 0.5 m)` snap; with the bank cut by the band the 1.6 cm asymmetry is harmless. (iii) 13an (d) as stated (two pieces' border polylines gap → 0) is not a patch census — the band edges are 10 m apart with draped DEM between; the §38 (5) `seam_residual` family (each band-edge vertex vs its own DEM) IS the instrument; the polyline gap stays a scout reading. (iv) `HARD SET` 1 of 12,310 at 0.0363 m certified to 0.0154 by the projection, `LAG` 0.082 → 0.158 (150 new hard equalities on the map boundary) — SETTLED-WITH-RESIDUAL under 13ak; recorded. (v) The DEM-prep write of `index.json` under a `--tile` build in a lane worktree is the 12j class (the guard held; the frame degraded) — folded into the dumpguard chip's scope note. Merge follows LEMD byte-identity; the seam law then ships in 1.0.327.

## Tool: mesh_region_tris

| `Ortho4XP/tools/mesh_region_tris.py` | Built-mesh triangle count inside the airport bbox — the number load time tracks — and, with `--area-bands`, the triangle SIZE distribution beside it: the near-degenerate SLIVER class (< 0.1 m^2, which carries no visible ground and costs load time outright), the sub-texel classes, and the visible class, each with its ground cover, in and out of the box. The band edges default to `0.1,1,TEXEL` and the literal `TEXEL` resolves to one orthophoto texel^2 at the bbox's mid latitude (ZL16 = 2.0662 m / 4.269 m^2 at HECA), because a triangle finer than a texel cannot be resolved by the imagery at all — that is the honest floor for "invisible geometry". `--json` stamps the bbox, the ZL and the edges beside the counts. Absorbed 2026-08-07 from `tmp/density_audit/meshtexel.py` on its second use (the sliver spec's phase-B interventional read) — promote-on-reuse, the near-fit extended rather than forked. **A/B WARNING, stated by the tool itself:** derive the bbox with `--bbox`, never `--patch-osm` — two arms' patches give two different boxes, which is two populations. `--aspect` adds the SHAPE half beside the size half: the ratio `longest edge / (2*sqrt(3) * inradius)` (1.0 = equilateral, rising without bound as a triangle degenerates) as p50/p90/p99/max over the in-bbox triangles, plus the NEEDLE count at `--aspect-flag` (default 20.0, a REPORTING threshold and an assumption, never a law — two runs quoted at two thresholds are not comparable). Reach for it whenever the question is "did this change mint a LONG-TRIANGLE artifact class", which the area bands structurally cannot answer: a 40 m x 0.5 m needle and a 4.5 m equilateral have the same area and land in the same band. Added 2026-08-08 (fabricA, THE FABRIC MODEL Phase A) because that round's acceptance is stated in exactly those terms; measured HECA in-bbox control p50 2.04 / p99 23.35 / max 975.53, sparse arm 2.02 / 22.76 / 975.53. Twin: `tests/test_mesh_region_tris.py` (a hand-built MEDIT mesh of deliberate areas; the published texel constants; every band-spec refusal; the aspect normalisation proven on a known equilateral and a known needle, and proven to SEPARATE two same-area triangles the bands cannot). **`--interp-alt-audit` (2026-08-28, the CYXY INTERP_ALT round) answers "who owns this mesh's altitudes" in the THREE artifacts it actually lives in, from the one MEDIT parse this tool already owns** (`--inputs PREFIX` for the build's `.node`/`.poly`, tile origin parsed from the mesh name or `--tile LAT LON`): the SEAL — is every INTERP_ALT seed in a BOUNDED face of the arrangement of the INTERP_ALT-marked segments, which is exactly Triangle4XP's own containment condition (regionplague crosses any segment sharing no bit with the flood); the PLAGUE — did every attr-8 triangle of the built mesh land inside that same envelope, i.e. did the marks survive the CDT; and the DOMAIN — how far does the CONNECTED attr==8 sub-mesh that `O4_Mesh_Utils` harmonic-extends the patch ring altitudes over (R18-1b) reach beyond the patch coverage, component by component, with each component's patch-valued anchor count and span. Reach for it whenever a mesh contradicts its own `.alt`: reading only one of the three is how +60-136 was first attributed to a plague leak that this audit refuted in 1 s — seal 0 of 578 seeds unsealed, plague 0 of 22,923 triangles outside the envelope, domain ONE 10.2 km component carrying all 6,677 patch-valued vertices against a 1.94 km^2 patch coverage (the town's levelled road network, welded to the airport wherever a road touches it). `--json` stamps every count. | **`--water-audit` (2026-09-09, lane `v2water`, owner RULINGS 09m) answers "is the water flat, and at its datum" from the mesh alone**: over every triangle carrying a water bit (WATER|SEA|SEA_EQUIV — the mask is twin-asserted against `O4_Vector_Utils.Vector_Map.dico_attributes`, never a second spelling), how many water VERTICES stand at 0.000 and how many do not with the commonest values named, how many water TRIANGLES span more than `--water-step-flag` metres corner to corner (a one-triangle step in open water is a sawtooth, not terrain), and the ATTRIBUTE HISTOGRAM beside them — which is the attribution: `SEA|INTERP_ALT` (10) means a patch seed reached that water and exempted it from sea levelling, bare `SEA` (2) means it did not. `--near LAT LON RADIUS_M` repeats the read at a site. Measured at OTHH on the owner's 1.0.297 build: 2,180 of 2,197 water triangles attr 10, every water vertex exactly 0.000 (699) or 3.962 (782), 1,692 stepped. Extended here rather than forked: the MEDIT parse, the tile-origin resolution and the JSON stamping are this tool's already. Twin: `tests/test_mesh_water_audit.py` (a hand-built mesh of one flat sea triangle, one 3.962 m stepped one, one inland body and one attr-8 land triangle that must not be counted). |

## Registered frames: LEMD

LEMD  capture  base ec8723e9   lane v2roadcap        2026-09-13T12:06:34  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/rw/cap/LEMD.pkl  — the v2roadcap-era LEMD capture used by scout v2unsettled2
LEMD  capture  base 864e7577   lane v2settle         2026-09-13T12:06:34  /private/tmp/claude-501/-Users-noah-XPTerrainBuilder/3fc455a9-745d-4126-a4c3-38d52975e33b/scratchpad/v2settle  — fresh main capture + per-law arms + logs (13ak)

## Standing discipline (unchanged for every lane; the long form is `.claude/agents/lane.md`)

- Worktree: `tools/harness/lane_worktree.sh up <lane> <base sha>`; branch `claude/<lane>`.
  `git merge main` FIRST if main has moved past the base sha.
- `Ortho4XP/venv/bin/python tools/blast.py <file>` before editing each source
  file; files under 1,000 lines.
- ONE closing build through the harness (v2 is the only engine, RULINGS
  2026-09-13au — there is no `--engine` flag); base arms cut with
  `git archive <sha> src`, never another live checkout.
- NEVER write `/Users/noah/XPTerrainBuilderData` or `/Users/noah/X-Plane 12/Custom Scenery/`;
  no `--refresh-data`; every run prints `[guard] shared repo UNCHANGED`;
  lane-local `O4_DSF_CACHE_DIR` / `O4_AIRPORT_MOD_CACHE_DIR` under your scratchpad.
- Any wait loop is bounded (`timeout N` or `$SECONDS`); prefer foreground.
- Suite from `Ortho4XP/` twice: `venv/bin/python -m pytest -q --no-header -p no:cacheprovider -rfE tests/auto_patch_v2 tests/test_harness.py tests/test_role_edge_census.py`.
- Attempt cap two per rule; a bar that moves backwards twice → delete the code, report the measurement.
- Consumer census (RULINGS 2026-08-30l) in the spec MEASURED block before any consumer is edited.
- Extend tools, never fork; INDEX row + twin for any new option. Do NOT merge into main; do NOT write RULINGS.
- Read law with `tools/docq.py spec '§N'`, `tools/docq.py ruling 13xx`, `tools/docq.py index <name>`;
  find captures with `tools/harness/frames.py list [ICAO]`; register yours when done.
- REPORT: branch + sha; per-bar before → after with the frame named; what you did NOT do;
  intent questions with their measurement; files touched.

