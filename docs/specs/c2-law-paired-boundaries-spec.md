# C2 — Law-paired shared boundaries (Fable, 2026-08-15 late)
Evidence: ledger C2/C6; C1 attribution (strip section); the mouthweld
7-site structural residual + R5c weld-site dossier; R5b's refutation
(never a frozen side). Population: strip_seam_tear 45 (worst 8.55 m /
174 % at -13273|-13275 — 3-row outlier site; 37 rows at the docketed
B2 site), adjacent_ground_tear 4, raoa 2, the R5c weld steps (2+1).
THE LAW: two surfaces sharing a boundary express it as LAW PAIRS in
the one grade graph — each shared-boundary vertex (or edge-interpolated
station) on side A is paired to its geometric partner on side B with
cap 0 (a weld pair, SHARED_VERTEX_TOL_M tolerance), solved TOGETHER —
never two independent conformances, never a frozen side, never an
emission-time re-seat racing the projection (the mouthweld structural
residual's named true fix: the node-vs-edge pair in the one graph).
Scope: graded_strip|graded_strip seams (adjacent_ground refs),
service_junction|service_road weld sites, strip|gap boundaries where
both sides are graded. NOT lawful-terrace boundaries (declared joints
stay exempt) and NOT airside|groundside interfaces where groundside
must not pull (pair direction: groundside vertex reads airside value
as an immutable boundary — the rim-pocket posture).
Acceptance: HECA strip_seam_tear 45→0 (the two pre-existing sites and
the B2 site all law-paired; any survivor dossiered), adjacent_ground_
tear 0, the R5c weld site 0; CYXY parity-or-better; airside deltas
attributed (pairing MOVES values — expect within_shape re-pricing,
report it); twins: (a) a shared seam solves to one value within tol,
(b) a declared terrace boundary stays exempt, (c) airside is never
pulled by a groundside pair. Materiality 0.01; attempt cap 2; STOP to
lead.
