# C3 — Aligned cross-sections everywhere (Fable, 2026-08-15 late)
Evidence: Grade Debt Ledger C3 (corrected). All 1,684 HECA groundside
transverse rows are SAME-SHAPE; the census casts true perpendiculars;
the tilt is longitudinal grade leaking through STATION MISALIGNMENT
between opposite edges (8 % × ~4 m ≈ the measured p50 5.1 % @ 7.5 m).
Junction rings carry 1,154 (no cross-section pairing at all).

THE LAW: every stationed road cross-section carries vertices on BOTH
edges at the SAME arclength (aligned partner feet — extend
`insert_service_lateral_nodes` to guarantee the pair, not just plant
feet); junction ring vertices within the station reach of a through
chain JOIN that chain's stations (the R5c co-level, extended from
value-sharing to foot-planting). The emitted mesh then interpolates
between equal-valued pairs and the lateral warp vanishes at the source.
Acceptance: HECA groundside transverse collapses to the real cliffs
(report the survivors with sites — they are C2/co-level residue to
adjudicate, target 0 via the junction rule); CYXY transverse likewise;
within_shape/steps must not rise (densification re-pricing reported);
airside byte-identical; twins: (a) every station has an aligned partner
on the opposite edge, (b) a junction vertex in reach joins the through
chain's station, (c) the misalignment fixture (offset vertices on an
8 % road) reads 0 % transverse after. Materiality 0.01; attempt cap 2;
deviations STOP to the lead.
