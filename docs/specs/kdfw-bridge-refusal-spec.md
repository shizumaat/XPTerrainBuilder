# KDFW — Refuse implausible deck contracts + deck-pin contradiction
# guard (Fable, 2026-08-15 late; from the KDFW attribution)
Mechanism (proven interventionally): the Aerosoft KDFW pavement inset
mesh (5 objects, ONE shared DSF anchor, 2,849.6 × 820.6 m pooled
"deck", contract_evidence=deck_profile_fallback, clearance 2.01 m
under a 4.2 m bound WARNed-and-emitted) classified DECK_CARRIED;
datum = one DEM sample at the anchor + 8 m = 183.29; 193 hard
deck-end pins entered the band seed set and inverted the final band
(650 nodes / 43 pairs, worst 1.996 m). The building-pad precedent
does NOT apply — no footprint exists at which 183.286 is correct.
1. REFUSE AT CLASSIFICATION (object_terrain_features._classify_
   contract): a DECK_CARRIED verdict whose contract_evidence is
   deck_profile_fallback is REFUSED when deck_length_m > 1000 OR
   deck_width_m > 60 OR deck polygon area > 40,000 m² (BOUNDS
   PROVISIONAL — owner ratification rides the round report; the KDFW
   slab exceeds all three by 3-7×); and a girder clearance below
   BRIDGE_ROAD_CLEARANCE_MINIMUM_M REFUSES the contract instead of
   WARN-and-emit (amendment A10 becomes a gate). A refused contract
   emits nothing (no trench, no corridor, no pins) and logs the
   refusal with its measurements.
2. DECK-PIN CONTRADICTION GUARD: before deck-end pins harden, run the
   EAT anchor-rect guard pattern (law_graph_budget.AnchorEnvelope.
   violation over the unified spine graph — the same build already
   refuses EAT pins this way); a pin whose envelope violates is
   refused and logged. Generalizes to every future bad pack datum.
Acceptance: KDFW builds rc=0 with the feature ON (no O4_OBJECT_
BRIDGE_TERRAIN=0), census reported; OTHH (the bridge fixture airport)
byte-identical or attributed — its REAL viaducts must keep their
decks (twin: a plausible-scale deck with real evidence still
classifies); the corpus-wide cache sweep for deck-scale outliers
(cheap read the attribution suggested) included in the report.
Materiality 0.01; attempt cap 2; STOP to lead.
