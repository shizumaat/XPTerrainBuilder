# SPJC seat/weld attribution (Fable brief, 2026-08-21; the last A4 gate)

Context: under A4 (lane/compose) SPJC reads 245 airside vs bar 189. 48 of
its 103 within_shape airside rows are SUB-5 M chords at > 2x cap (worst
35.31 % over a 1.30 m chord, |de| 0.460 m), 17 in the declared cluster at
(-12.021394, -77.110990) where ways -10113 / -10162 / -10698 / -10699
meet; the class as a whole GREW across the apron-law arms (battery 31 →
A4 48), so the apron law is not its author and may be its victim's
neighbour. SPJC is gentle (standing memory: big SPJC counts = bug /
instrument / remnant prior). This is ATTRIBUTION-FIRST: locate the
mechanism; fix only on the lane's-own-defect branch.

## Pre-registered reads (on the A4 SPJC patch + sidecar; no build)

1. THE ROWS. All 48 sub-5 m > 2x rows: site, ways, chord, |de|, grade,
   cap, door (ring-adjacent / nearest-spine / frontage / foreign), and
   whether each endpoint is a WELD (a node shared between two+ shapes —
   which shapes/roles) or interior. Cluster membership by distance to the
   named junction.
2. THE VALUES. For each row's endpoints: solved z, the shapes claiming
   the node, and each claiming shape's z at that position (emit
   consensus: did two authorities disagree and average? — the
   emit-consensus-mints-violations precedent). Establish whether the step
   exists in the SOLVER's values (pre-emit, from the sidecar/bake frame)
   or is MINTED at emit (weld/consensus/decimation) — the decisive split.
3. THE HISTORY. census_rows_diff battery → A3 → A4 restricted to the
   class: are these the SAME rows drifting up, or churn (different rows
   each arm)? A class that grows as neighbouring constraints relax points
   to the projection redistributing residual into short edges; same-rows
   points to a fixed seat defect.
4. THE GEOMETRY. At the junction: how many nodes within 5 m, from which
   ways, seated by which mechanism (seat stamps / weld inserts /
   backfill)? Is this the join_snap_t sliver class (runway_segments) or
   the nid-level final weld (layout.py:2325) inserting at interpolated
   altitudes between disagreeing neighbours?

## Pre-delegated decision tree (pick exactly one)

* EMIT-MINTED (solver values lawful, emit weld/consensus makes the step):
  defect in the emit weld path — fix in the lane (the weld must carry ONE
  authority's value per the seat-is-the-weld ruling 2026-08-08), one SPJC
  build, re-census; HECA/CYXY must not regress (fastpath re-price first).
* SOLVER-SEATED (two shapes solved to different z at a shared position —
  a lockstep/identity fork): report with the claiming shapes and their
  stages; likely the C2 law-pair territory (non-contact pairs docket) —
  STOP for the lead, no fix.
* PROJECTION RESIDUAL (values move arm-to-arm, rows churn, step present
  pre-emit but only after the transect/staged passes): report which
  constraint's relaxation feeds it — STOP for the lead.
* Anything else: STOP with row-level evidence.

## Constraints

Read-first (the A4 artifacts are on disk); a build is lawful only for the
emit-minted branch's verification arm. Materiality 0.01 m; attempt cap 2;
never write the shared repo; no timing; blast.py before editing; twins for
any fix; commit on lane/compose. Acceptance for a fix: SPJC airside ≤ 189
with the weld class collapsed, CYXY/HECA fastpath re-price unchanged.
